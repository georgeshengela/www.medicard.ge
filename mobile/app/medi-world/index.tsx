import ExploreScreen from './explore';

/** Medi World opens on the interactive map. Garden, Companion, Adventure, and Friends stay on overlay chips. */
export default function MediWorldScreen() {
  return <ExploreScreen hub />;
}
