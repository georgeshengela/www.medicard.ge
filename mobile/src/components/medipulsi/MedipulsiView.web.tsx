import React from 'react';
import {Redirect} from 'expo-router';
/** Legacy entry retained for older in-app references. */
export default function MedipulsiView(_props:{userId:string}){return <Redirect href="/run"/>;}
