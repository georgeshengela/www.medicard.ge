/** Consistent home screen spacing — use everywhere on the hub. */
export const HOME_SPACE = {
  screenPad: 16,
  topInset: 6,
  sectionTop: 16,
  sectionLabelBottom: 8,
  blockGap: 8,
  heroBottom: 12,
  cardPad: 12,
  cardRadius: 16,
  iconMd: 40,
  iconSm: 36,
} as const;
