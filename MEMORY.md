# Medicard.GE — agent memory

## Mobile app versioning (mandatory)

`expo.version` is the **public five-part identity**: Instagram-style `G.0.0.B.R`, currently **`1.0.0.7.79`**. Same string is Android `versionName`, in-app wordmark, and `/api/app/status`. Apple `CFBundleShortVersionString` is **`expo.ios.version` = `1.7.79`** (`G.B.R`); plugin `withIosMarketingVersion` is the last-write guard. Never set admin `minAppVersion` to `1.7.72` or `1.7.77`. Native integers in `app.json` (`66`) are a local floor only — EAS production `appVersionSource: remote` + `autoIncrement` is the uploaded counter (remote was **2** on 2026-09-11; first production build will be 3 unless remote is raised). Cycle phase numbers stay in contracts / QA only.

| Change type | Rule | Example |
| --- | --- | --- |
| Small features, fixes, polish, copy | revision +1 | `1.0.0.7.68` → `1.0.0.7.69` |
| Native / store train | train + 1 and bump `buildNumber` / `versionCode` | `1.0.0.7.68` → `1.0.0.8.0` |
| Rare product generation | first number + 1 | `1.0.0.7.68` → `2.0.0.0.0` |

In-app display and `/api/app/status` use `APP_VERSION` from `mobile/src/lib/appVersion.ts` (`expo.version`). Do this on store-facing mobile changes. Public identity **1.0.0.7.79**. iOS marketing **1.7.79**. Medi World Phase 43.1 **PASS** (Hub reward cache, Android evidence); not committed/pushed/deployed. Phase 44 not started. Former internal 3-part ledger ended at **65.0.3**. Brief three-part `expo.version` **1.0.66** was replaced by the five-part identity (app is unpublished). Previous: **65.0.1** (**CYCLE PHASE 42 — EXPLICIT POSTPARTUM RETURN TO STANDARD CYCLE TRACKING FINAL-FROZEN**). Previous freeze: **64.0.1** (Georgian copy polish on frozen **CYCLE PHASE 41**). Previous freeze: **64.0.0** (**CYCLE PHASE 41 — OWNER-CLASSIFIED RETURN OF PERIOD FINAL-FROZEN**). Previous freeze: **63.0.0** (**CYCLE PHASE 40 — POSTPARTUM JOURNAL & COPY ISOLATION FINAL-FROZEN**). Previous freeze: **62.0.0** (**CYCLE PHASE 39 — POSTPARTUM DOCTOR SUMMARY CONTEXT FINAL-FROZEN**). Previous freeze: **61.0.0** (**CYCLE PHASE 38 — POSTPARTUM MODE FOUNDATION FINAL-FROZEN**). Previous freeze: **60.0.0** (**CYCLE PHASE 37 — OWNER-ENTERED PRENATAL VISIT PLACE FINAL-FROZEN**). Previous freeze: **59.0.0** (**CYCLE PHASE 36 — OWNER-OPTED EXACT-TIME PRENATAL CARE REMINDERS FINAL-FROZEN**). Previous freeze: **58.0.0** (**CYCLE PHASE 35 — OWNER-ENTERED PRENATAL APPOINTMENT TIME FINAL-FROZEN**). Previous freeze: **57.0.0** (**CYCLE PHASE 34 — OWNER-OPTED PRENATAL CARE CALENDAR EXPORT FINAL-FROZEN**). Previous freeze: **56.0.0** (**CYCLE PHASE 33 — USER-OPTED PRENATAL CARE REMINDERS FINAL-FROZEN**). Previous freeze: **55.0.0** (**CYCLE PHASE 32 — PREGNANCY PRENATAL CARE PLANNER FINAL-FROZEN**). Previous freeze: **54.0.0** (**CYCLE PHASE 31 — OBSERVATION DATA TRANSPARENCY & COMPARISON EXPLAINABILITY FINAL-FROZEN**). Previous freeze: **53.0.0** (**CYCLE PHASE 30 — EXPOSURE-AWARE TWO-WINDOW OBSERVATION COMPARISON FINAL-FROZEN**). Previous freeze: **52.0.0** (**CYCLE PHASE 29 — EXPOSURE-AWARE OBSERVATION RATES FINAL-FROZEN**). Previous freeze: **51.0.0** (**CYCLE PHASE 28 — EXPLICIT OBSERVATION ASSESSMENT & EXPOSURE FOUNDATION FINAL-FROZEN**). Previous freeze: **50.0.0** (**CYCLE PHASE 27 — MODE CAPABILITY & PRESENTATION INTEGRITY FINAL-FROZEN**). Previous freeze: **49.0.2** (**CYCLE PHASE 26 — DENOMINATOR-SAFE PERIMENOPAUSE OBSERVATION SUMMARIES FINAL-FROZEN**). Previous freeze: **48.0.0** (**CYCLE PHASE 25 — DOCTOR SUMMARY PERIMENOPAUSE TRACKING CONTEXT FINAL-FROZEN**). Previous freeze: **47.0.0** (**CYCLE PHASE 24 — PERIMENOPAUSE MODE FOUNDATION FINAL-FROZEN**). Previous freeze: **46.0.0** (**CYCLE PHASE 23 — DENOMINATOR-SAFE PREGNANCY OBSERVATION TRENDS FINAL-FROZEN**). Previous freeze: **45.0.0** (**CYCLE PHASE 22 — PREGNANCY SYMPTOM & BODY CHANGES EXPERIENCE FINAL-FROZEN**). Previous freeze: **44.0.0** (**CYCLE PHASE 21 — PREGNANCY TIMELINE & MILESTONES FINAL-FROZEN**). Previous freeze: **43.0.0** (**CYCLE PHASE 20 — DOCTOR SUMMARY PREGNANCY HEADER FINAL-FROZEN**). Previous freeze: **42.0.0** (**CYCLE PHASE 19 — PREGNANCY BABY SIZE & WEEK-BY-WEEK VISUAL GUIDE FINAL-FROZEN**). Previous freeze: **41.0.0** (**CYCLE PHASE 18 — PREGNANCY MODE FOUNDATION FINAL-FROZEN**). Previous freeze: **40.0.1** (**CYCLE PHASE 17 — TTC COLD-START RELIABILITY**; Phase 16 TTC product remains frozen). Previous freeze: **40.0.0** (**CYCLE PHASE 16 — TTC MODE FOUNDATION FINAL-FROZEN**). Previous freeze: **39.0.29** (Phase 15 clinician locale rendering KA/EN/FR/RU). Previous freeze: **39.0.28** (Phase 14 doctor-summary observation inclusion). Previous freeze: **39.0.27** (Phase 13 Journal observation trends). Previous freeze: **39.0.26** (Phase 12 rich-tracking). Android Cycle calendar / prediction-history / observation-registry writes remain frozen. **iOS native QA is deferred by product decision** — not a blocker. TTC is a live presentation mode (`TRY_TO_CONCEIVE`); Pregnancy is a live explicit-entry mode (`PREGNANCY`) with `CyclePregnancyEpisode`. Perimenopause is a live explicit-entry tracking mode (`PERIMENOPAUSE`) — not a diagnosis. Postpartum is a live explicit-entry tracking mode (`POSTPARTUM`) — not a pregnancy outcome. OpenRouter Cycle AI remains out of scope. Previous note: Mobile app is at **39.0.13**. Previous note: **36.3.4**. Home scroll order is owned by `buildHomeSectionOrder` (`mobile/src/lib/home/homeSectionOrder.ts`): NextDose → Steps → Hydration → Weight → Quest+Companion → Cycle → Lab → Weather → Run → Symptom → Analysis → Consilium → Recent — order/presence only; Quest is `MEDI_ENGAGEMENT` after TODAY (health-first). No section redesign. (**MEDI COMPANION V1 — FINAL-FROZEN** via Phase 9.3 native QA; artifacts in `qa/phase-9.3/`). Offline is a floating XP-style chip (not a layout-pushing banner); `useOffline` needs 2 failed `/health` pings. Run hub hero is the desktop sprint reel. Run target sheet hides the floating tab pill (`hideFloatingTabBar`) so AppChromeOverlay cannot cover **სამიზნის მონიშვნა**. Medi Quest Rewards Store v1 is production-live on Render (`0328a76`; prior `454bfe5` failed boot missing achievement socket exports). `/api/rewards*` auth-gated; release = `prisma generate` + seed (**not** `db push`). Wallet `walletSourceLabel` maps SYSTEM/unknown honestly; same-device Coin sync via `invalidateMediCoinBalance` / `publishMediCoinBalance` (cross-device = focus/foreground only). ACTIVE freeze: `MEDI_THEME_7D` / `MEDI_PROFILE_STYLE_30D`; Premium + `PARTNER_TEST_10` DRAFT. Docs: `server/docs/rewards-store-deploy.md`. No cash/crypto/P2P/Store marketing pushes. Phase 6 Smart Quest × Notification Brain: Engage family `questSmart` (client-side Brain remains sole scheduler; max 1 Smart Quest push per local Quest day + 20h cooldown). Five candidate types in `mobile/src/lib/quest/questSmartEngage.js` (NEAR_COMPLETE / GOOD_WEATHER_WINDOW / COMEBACK / MORNING_PLAN / WEEKLY_PROGRESS) with explainable scores, mandatory revalidation (`QUEST_*` reason codes), deep link `/medi-quest?from=push`, DEV weather fixture (never writes LIVE cache). Pain uses existing Brain `loggedPain` only. Phase 5 Smart Quest Engine: `server/src/lib/smartQuestEngine.js` (engine v1) personalizes STEPS targets only — 14-day median baseline (min 3 valid days, 100k sanity ceiling), deterministic bands + roundTo250, hard max 10000, STRUGGLING (-10%, floor 2000), comeback cap, weekly roundTo1000(B*7*0.90) clamp 21k–60k. Target + `metadata.smart` freeze per periodKey; rewards NEVER vary with target (daily_steps always 50/30). `reconcileQuestEligibility` cancels ACTIVE quests on capability/goal loss (restores same period, never touches COMPLETED/CLAIMABLE, never punishes streak). API exposes only targetSource/difficulty/reasonKey. Mobile smart context (`src/lib/quest/smartContext.js`): one Medi line per movement card, priority pain > comeback > evening > near-completion > weather > reason; "რატომ ეს მიზანი?" bottom sheet; copy in catalog `smart`/`whyTarget` ka/en/fr/ru with deterministic rotation. Weather is presentation-only (mobile useWeather); server assigns weather-blind. No new pain medical pipeline — Brain cycle `loggedPain` is the safe boolean for Quest Smart suppressions. Medi Quest Phase 4 Achievements (29.0.x) extends the frozen Quest UI v2: `AchievementDefinition`/`UserAchievement` tables (additive SQL, `server/prisma/phase4-achievements.sql`), 50-item catalog in `server/src/lib/achievementDefs.js` — **every achievement carries its own explicit rewardXp/rewardCoins locked by the Phase 4 spec (snapshot-tested in `achievements.test.js`); rarity is presentation/difficulty metadata only and must NEVER derive rewards**. `maxSingleAchievementXp` is 2000 (STREAK_365 pays 1500 XP; invalid defs are rejected, never clamped). Evaluator/claim/reconcile in `server/src/lib/achievements.js` hooked after quest complete/claim, API at `/api/achievements`, Socket.IO `achievement:unlocked|claimed`. Mobile: `/medi-quest/achievements` collection screen + hub preview strip + rarity-tinted unlock toast in `QuestHost` + level-up cascade reuses the global sheet. Rarity palette in `questTokens.ts` `QUEST.rarity`. Achievement copy is pattern-based ka/en/fr/ru in `src/i18n/quest/achievements.js`. Medi Quest level-up is Figma `8853:146377` (centered modal, 320px dotted-halo score disc, green check badge, navy X below the card) — copy stays Medi, never Nightingale. Medi Quest Mobile UI v2 (28.0.0): level ring + stat chips + segmented daily bar + `QuestClaimButton`; Home Medi Quest sits **directly under the header score card**. Quest primitives live in `src/components/quest/` and tokens in `src/theme/questTokens.ts` — reuse them, do not restyle. NativeWind v4 gotcha: a `Pressable` with a **function** `style={({pressed}) => ...}` and no `className` drops its styles on Android — use a static `style` + `className="active:opacity-75"`. Reanimated: never put a static `opacity` on the same `Animated.View` that has `entering`/`layout` (wrap it in an inner View). Expo Go Android must import notifications via `@/lib/expoNotifications`, not `expo-notifications`. (Expo SDK 57 / React Native 0.86.3 / React 19.2). Weather Wellness sits on Home as a compact Medi card (not a weather app): Open-Meteo from existing GPS, 20-min cache, rule-based outdoor window + copy, Brain family `weatherWellness`. Location is a first-class permission: onboarding screen after notifications, one-time ask for existing users, profile permissions toggle, Georgian city + country flag on the profile hero, and live GPS while the app is open (`POST /api/location`, `UserLocation` table, Nominatim reverse-geocode in ka on first grant or a 2 km move). No background location. Outcome/permission/activity headers sync from **24.0.0+**; Brain decisions from **23.0.3+**. `/week` writes `weekly_report_opened` only. Splash first beat is Figma `8846:211832` teal rings + vector `MedicardLogoMark` inside a filling circle, then the water-fill percent. Android Health Connect needs `android.permission.health.*` + `<queries>` for `com.google.android.apps.healthdata` in the **native** APK — the official Expo plugin only adds the rationale activity, so a JS reload will not fix "cannot connect" on a real Android device. Rebuild/install after plugin changes. After notifications, profile-setup **analyzing** is the last step: Figma `8846:211832` teal rings, avatar + Medicard mark + `{name} ვამზადებთ პროფილს`, then `finishOnboarding` → home. Asklepios score / results page is skipped in the live flow (results still exists for preview). Assessment steps use `useAssessment()` tokens (`tint` / `brandInk` / `hairline`) so dark is navy + mint, not light `#F0FDFA` boxes or `#0F766E` text. Home წონის კონტროლი keeps the Nightingale card size: weight + BMI index, WHO zone bar, category / healthy kg range / week delta. Title stays above the card. iPhone Camera Roll HEIC is transcoded to JPEG before upload; picker uses `preferredAssetRepresentationMode: Compatible`. Server also accepts HEIC/jpg aliases and sniffs magic bytes. Decode-lab chat (`/module/lab`) is a studio: camera stays available after the first shot, another-page prompt, live scan theater + sticky progress dock. **One day's lab test = one OpenRouter read** via `POST /api/ai/extract-lab` (all pages, 1 AI credit, no EvidenceMD). Parameters + printed ranges land in `/lab`. User can later tap **გაანალიზეთ Medi-სთან** for one EvidenceMD write-up (`POST /api/ai/explain-lab`, 1 credit). Imaging/skin still use `/api/ai/analyze-image`. **ლაბორატორია** hub is `/lab`: asks for the test date if OCR misses it, stores parameters by date (`medicard.lab.panels.v1`), Figma `8852:143210` date list with Normal/High/Low badges, tap a parameter for the trend chart. Home section title **ლაბორატორია** sits above the card. Public legal pages: `server/public/privacy.html` and `server/public/terms.html` (same landing chrome, FiraGO + Davit Guramishvili slogan only, light/dark). Sources: `scripts/privacy-source.md`, `scripts/terms-source.md`. Rebuild both with `node scripts/build-legal-pages.mjs`. Effective **4 სექტემბერი, 2026**. Controller listed as Medicard.GE / support@medicard.ge — add legal ID and street address when registered. Terms are 52 sections; do not claim the app is fully free (FREE/STANDARD/ULTIMATE exist). Profile tab shows account + health facts, in-app Privacy/Terms, support email, and Apple-required account delete (`DELETE /api/auth/me`). Public pages: `https://medicard.ge/privacy` and `/terms` after server deploy. Hidden unfinished Google sign-in for App Store. Cycle notes: Cycle hub ring is Figma `9001:283189` closed into a true circle (5-segment rose gauge, dotted track, white knob, faint Medicard mark *inside* the disc). Light Cycle chrome is blush (`#FFF7F8` / `#F43F5E` CTAs). Dark stays navy with rose accents. Today ring stays teal so today ≠ period. Fertile/ovulation stay violet estimates. Cycle **AI რჩევები შენთვის** is a featured Medi tip + stacked cards: title outside, phase chips, cycle-aware local advice merged with EvidenceMD (`cycleAdvice.ts`). /symptoms intro is a navy splash (`#030712`) with a 15s Red Bull-style reel (`intro-body.mp4`): woman basketball → knee bones/veins, woman sprint → abdomen anatomy, solo man bag-work → real shoulder strain (no CGI bone window). Brand `#14B8A6` / `#99F6E4`, CTA `#0D9488`. Hydration hub is `/health-metrics/hydration` (glass, calendar week, log, history, details, level). Home shows **ჰიდრატაცია** title above Figma `9283:202564` (drop, ml + goal, hatched teal bar), under activity/steps. Medi / analysis (`withPatientAiContext`) now get health profile + last 14 daily metrics including **hydrationMl, steps, weight, sleep, HR, BP, nutrition, active minutes** — needs a server deploy. Step goals stay local; daily steps still go to AI. Render web export needs `expo-video` in `mobile/package.json` whenever it is listed in `app.json` plugins. Don't commit a screen that imports a new component without the component file. Profile-setup results at `/profile-setup/results` is a live analysis: `POST /api/health-profile/onboarding-analysis` with `{ force: true }` recomputes from profile + extraAnswers + 14-day metrics + scheduled meds + cycle mode. Heuristic score moves with the data; AI can only nudge ±12. Results has **ხელახალი ანალიზი** in place. Completed users can stay on results (root layout already allows `onResults`). Home hub sections are always **title then content** (`HomeSectionTitle` above the card, never the section name inside it) — same as შემდეგი მიღება, წონის კონტროლი, აქტიურობა. Daily login awards **5 points** inside `GET /api/auth/me` on every app open (Tbilisi day, unique per user). Streak screen is Figma `11425:139549` at `/profile/streak`. Profile tab / permissions dark chrome uses the same cool gray-950 stack as home and meds (`#030712` page, `#111827` cards, `#1F2937` chips, filled CTAs `#0D9488`). Medications hub uses a normal title (`მედიკამენტების ტრეკერი`) — no home date/hello/avatar header. The bottom tab pill overlays content with no page-color strip around it. Dark theme is cool gray-950 (`#030712` page, `#111827` cards, teal `#14B8A6`) — not teal charcoal. Overlay modals always fade (`APP_MODAL_PROPS` in `mobile/src/components/ui/appModal.ts`) — never `animationType="slide"` on a transparent Modal. Steps, meds, symptoms, plans, assessment, and profile-setup all follow the same light/dark token hooks as auth/home. Steps goals are a deadline campaign (not only the daily 10k chart): set target + deadline + weekday reminders at `/health-metrics/steps/goal` (Figma 8924:166715 / 8921:69885 / 8851:142819). Home წონის კონტროლი tap opens the per-account weight hub (`/health-metrics/weight`, Figma 8927:182138) — 64px teal circle, Medi tips via `POST /api/ai/weight-advice` (OpenRouter, 1 credit, cached per day), history swipe-to-delete (red 48px trash), and a goal wizard (target / pace / reminder / confirm / progress / completed). Logs and goals use `getScopedPreference` (`medicard.weight.logs.v1`, `medicard.weight.goal.v1`). Hub pencil still opens the add-weight sheet. Needs a server deploy for live Medi tips. Home page no longer shows the health-metrics privacy disclaimer or the compact package/quota banner. Symptom anatomy (`/symptoms/body`) uses Nightingale Human Body SVGs (`9192:5539`) as tappable regions with pinch/zoom — not PNG + dots. **Phase 9 Medi Companion** (`/medi-companion`): soft teal SVG evolved from `robot-2.svg`, journey path + collection equip, home chip beside Quest (not a second dashboard), Talk → `/chat/doctor`. Client `api.mediCompanion` ↔ `/api/medi-companion*`. Does not touch Rewards Store / Partner / Brain / quest economy.

## Admin backend

- UI: `http://localhost:4000/admin` (prod: `https://medicard.ge/admin`). Public `https://medicard.ge/` is a Georgian marketing landing (`server/public/index.html` + `landing.css` / `landing.js`) — no web login. Real app screens in `/screens/`. Light/dark switcher, solid colors only (no CSS gradients), feature encyclopedia + plan compare (includes **ამინდი და Medi**). Product is mobile-only. Public **კალკულატორები** at `/calculators` (10 cycle/pregnancy tools; browser-only math; rebuild with `node scripts/build-calculator-pages.mjs`).
- Dashboard is the Operations & Intelligence Command Center (`#/overview`): real Tbilisi-day aggregates. **DAU/WAU/MAU = AppActivity** (authenticated open / `/api/auth/me` or `/api/check-in/session`, one row per user per Tbilisi day). DailyCheckIn is the login bonus only — not a session metric. Users `today` / `inactive7` / `inactive30` use AppActivity `lastActiveAt`, not `lastCheckInDate`. Version gates live in `appVersionPolicy.js`. Cache `admin.css` `?v=ops8`, `admin-v24.css` `?v=ops11`, `admin-v25.css` `?v=ops23`, `admin-v26.css` `?v=v26.5`, `admin.js` `?v=v27.7`, `ops-center.js` `?v=v26.10`, V3 shell `?v=v3.65`. User profile `#/users/:id` is full-width. Sidebar brand is Georgian **მედიქარდი** + mobile logo mark (FiraGO `case` on logo only); nav labels are normal sentence case, not ALL-CAPS. Overview Command Center is full-width with live **ახლა აპში** (`ops:live.onlineNow`), OpenRouter/EvidenceMD/app version infra strip, and Socket.IO KPI patches. Admin dates never use English months — `fmtDate` / `fmtDateShort` write `5 სექტემბერი, 2026`. Overview live updates use Socket.IO (`/socket.io`) — do not re-render `#/overview` on a timer. Last-active is `AppActivity.lastAt` from app heartbeats (screen + 15s + resume) on **24.0.2**; admin polls every 8s. Analytics cache is 8s. Restart API + ship the app build. Weekly **opened** is `/week` view only; **generated** is Brain weekly SEND. Admin V2.2 is operator UX. **V2.3** is user investigation + decision debugging. **V2.4** is visual-only: labeled 212px sidebar, design tokens, KPI strip, chart language, drawers/tables. **V2.5** is page-by-page visual redesign (`admin-v25.css`) — no new metrics or endpoints. Hard-refresh `/admin?v=ops12`. **ფაზები** (`#/cycleqa`) is the QA phase board: scans `qa/` + freeze catalog (Cycle 5–39, Medi Companion, Admin V3, Home reorder). `GET /api/admin/cycle-qa`. Screenshots are admin-auth blob fetches — not live user health. iOS QA deferred is product, not a blocker.
- Users registry (`/admin/#/users`): V3 redesign (`?v=v3.65`) — KPI cards, chip filters (status/package/activity), sortable table with feature chips + quota + platform/version, numbered pager (20/page). User page `#/users/:id` has hero + KPI strip + restyled investigation tabs (მიმოხილვა / აქტივობა / პროდუქტის გამოყენება / შეტყობინებები / მოწყობილობები / აუდიტი). **Tab selector and tab content must share the same left/right edges** (see `AGENTS.md` Admin tab shell width). Privacy-safe: no chat text, med names, or health values. Decision drawer enriches platform/version from Outcome → nearest AppActivity → latest before → unknown (`contextSource` is explicit). Unknown hashes show a console 404. Decision IDs are clickable both ways.
- Admin push (`#/push`) keeps the studio and adds a **Brain** tab (funnel, suppression reasons, fatigue vs preference, engagement heatmap, decision explorer). Decisions sync from app **23.0.3+** via `POST /api/push/decisions` (sanitized: no chat text, no health values). Stats now include iOS/Android/Web counts. Expo tokens: **iOS confirmed 2026-09-05**. Android needs FCM V1 + `google-services.json` + a native rebuild; Expo Go cannot do Android remote push from SDK 53. In-app toggle is `medicard.push.optedIn`. Medi reminder copy is warm first-person; empty `{dosage}` / `{place}` / `{doctor}` tidy themselves; `cycle-masked` stays lock-screen safe. Technical keys stay behind Advanced. Broadcast campaigns stay separate from local phone reminders.
- Overview also shows live OpenRouter USD balance (`GET /api/v1/credits`) and EvidenceMD status + Medicard usage (they have no public wallet API)
- Women's cycle module (**FEMALE only**): mobile `/cycle`, API `/api/cycle` — period predictions, daily symptom log, TTC (BBT/mucus/OPK/pregnancy-test observations), pregnancy week tracker, Perimenopause tracking (explicit `PERIMENOPAUSE` mode, not a diagnosis), **Postpartum tracking (explicit `POSTPARTUM` mode, not a pregnancy outcome)**, **authenticated** partner share (`/share/cycle/[code]`, 30-day hash token, owner revoke + permissions), doctor summary → AI chat prefill. Mobile app **1.0.0.7.68**. **CYCLE PHASE 42 — EXPLICIT POSTPARTUM RETURN TO STANDARD CYCLE TRACKING FINAL-FROZEN.** Owner may explicitly leave POSTPARTUM for `TRACK_PERIOD`. Mode and forecast readiness are separate: classified postpartum periods can become factual history, but next-period / late / fertile / ovulation stay gated until two valid new intervals exist. No default-28, stored average, or pre-pregnancy shortcut. While `forecastEligibility.allowed === false`, Cycle/Home/TTC gauges hide stored/default cycle-length chrome (`28-დან`) and show `ციკლის ახალი ისტორია გროვდება`. Android shots `qa/cycle-phase42-postpartum-return-to-track/`. Contract: `docs/CYCLE_POSTPARTUM_RETURN_TO_TRACK_CONTRACT.md`. Previous freeze: **64.0.0**. **CYCLE PHASE 41 — OWNER-CLASSIFIED RETURN OF PERIOD FINAL-FROZEN.** Owner may mark a postpartum bleed episode as their period. Unclassified postpartum bleeding stays unknown and excluded from engine input. POSTPARTUM forecast stays suppressed. No fertility/ovulation inference. Android shots `qa/cycle-phase41-postpartum-period-classification/`. Contract: `docs/CYCLE_POSTPARTUM_PERIOD_CLASSIFICATION_CONTRACT.md`. Previous freeze: **63.0.0**. **CYCLE PHASE 40 — POSTPARTUM JOURNAL & COPY ISOLATION FINAL-FROZEN.** POSTPARTUM Journal and `/cycle/summary` preview shell no longer show menstruation empty/history copy. Bleeding remains a fact (`სისხლდენა`). Phase 39 doctor JSON/PDF unchanged. Forecast arithmetic unchanged. Android shots `qa/cycle-phase40-postpartum-copy/`. Contract: `docs/CYCLE_POSTPARTUM_COPY_ISOLATION_CONTRACT.md`. Previous freeze: **62.0.0**. **CYCLE PHASE 39 — POSTPARTUM DOCTOR SUMMARY CONTEXT FINAL-FROZEN.** Additive `postpartumContext` on existing `GET /api/cycle/doctor-summary` when mode is POSTPARTUM and an ACTIVE episode exists (owner-entered reference only; not a pregnancy outcome; not a recovery score). Android shots `qa/cycle-phase39-postpartum-doctor/`. Contract: `docs/CYCLE_POSTPARTUM_DOCTOR_SUMMARY_CONTRACT.md`. Previous freeze: **61.0.0**. **CYCLE PHASE 38 — POSTPARTUM MODE FOUNDATION FINAL-FROZEN.** Explicit owner-selected mode only. Optional owner-entered `referenceDate` (not birthDate). `CyclePostpartumEpisode` one ACTIVE. Forecast arithmetic unchanged; postpartum-stamped flow excluded at the engine input boundary. Android shots `qa/cycle-phase38-postpartum/`. Contract: `docs/CYCLE_POSTPARTUM_MODE_CONTRACT.md`. Previous freeze: **60.0.0**. **CYCLE PHASE 37 — OWNER-ENTERED PRENATAL VISIT PLACE FINAL-FROZEN.** Optional owner `plannedPlace` (max 160 Unicode) on a planned prenatal date. User-entered visit-place text only — no geocoding, maps, clinic search, or location permission. Does not enter reminders, OS Calendar, partner, Medi, doctor summary, or analytics. Android shots `qa/cycle-phase37-visit-place/`. Contract: `docs/PREGNANCY_CARE_VISIT_PLACE_CONTRACT.md`. Previous freeze: **59.0.0**. **CYCLE PHASE 36 — OWNER-OPTED EXACT-TIME PRENATAL CARE REMINDERS FINAL-FROZEN.** Optional `EXACT_TIME` Medicard reminder only after the owner enters both `plannedDate` + `plannedTime` and explicitly selects exact mode (leads: at time / 30 min / 1 hour / 2 hours). Adding time never auto-upgrades a Phase 33 DATE_BASED 09:00 reminder. Notification Brain remains sole delivery. Android shots `qa/cycle-phase36-exact-time-reminder/`. Contract: `docs/PREGNANCY_CARE_EXACT_TIME_REMINDER_CONTRACT.md`. Previous freeze: **58.0.0**. **CYCLE PHASE 35 — OWNER-ENTERED PRENATAL APPOINTMENT TIME FINAL-FROZEN.** Optional owner `plannedTime` (`HH:mm`) on a planned prenatal date. Date-only remains default all-day export. Timed OS events only after an owner-entered clock; 30-minute technical calendar end, never shown as appointment length. Phase 33 reminders ignore `plannedTime`. Android shots `qa/cycle-phase35-appointment-time/`. Contract: `docs/PREGNANCY_CARE_APPOINTMENT_TIME_CONTRACT.md`. Previous freeze: **57.0.0**. **CYCLE PHASE 34 — OWNER-OPTED PRENATAL CARE CALENDAR EXPORT FINAL-FROZEN.** One-way local OS Calendar export of a user-planned `plannedDate` only (`expo-calendar/legacy`, all-day, generic title default, no catalog-window events, no OS alarms, no calendar scanning). Device-local ownership `medicard.pregnancy.careCalendar.v1.{userId}` — not in the health API. Android shots `qa/cycle-phase34-care-calendar/`. Contract: `docs/PREGNANCY_CARE_CALENDAR_EXPORT_CONTRACT.md`. Previous freeze: **56.0.0**. **CYCLE PHASE 33 — USER-OPTED PRENATAL CARE REMINDERS FINAL-FROZEN.** Explicit opt-in for a user-planned care-plan date only (`USER_PLANNED_EVENT`). Notification Brain sole delivery (`pregnancy_care_plan` / `pregnancyCareReminder`). No catalog-window or “scan is due” pushes. Android shots `qa/cycle-phase33-care-reminders/`. Contract: `docs/PREGNANCY_CARE_REMINDER_CONTRACT.md`. Previous freeze: **55.0.0**. **CYCLE PHASE 32 — PREGNANCY PRENATAL CARE PLANNER FINAL-FROZEN.** Source-backed informational planner (`prenatal-care-v1`); episode-scoped `PregnancyCarePlanItemState`; compact `carePlannerSummary` + `GET/PUT /api/cycle/pregnancy/care-plan`; Overview card + `/cycle/pregnancy/care-plan`. Not medical orders, diagnosis, risk score, AI, partner, doctor summary, or a second GA engine. Android shots `qa/cycle-phase32-care-planner/`. Contracts: `docs/PREGNANCY_CARE_PLANNER_CONTRACT.md`, `docs/PREGNANCY_CARE_PLANNER_DATA.md`. Previous freeze: **54.0.0**. **CYCLE PHASE 31 — OBSERVATION DATA TRANSPARENCY & COMPARISON EXPLAINABILITY FINAL-FROZEN.** Additive `explainability` reasons on Pregnancy/Peri Journal expanded rows; one sheet explains assessed-day rates/comparisons/direction without coaching, streaks, thresholds, or diagnosis. Android shots `qa/cycle-phase31-explainability/`. Contract: `docs/CYCLE_OBSERVATION_EXPLAINABILITY_CONTRACT.md`. Previous freeze: **53.0.0**. **CYCLE PHASE 30 — EXPOSURE-AWARE TWO-WINDOW OBSERVATION COMPARISON FINAL-FROZEN.** Two adjacent non-overlapping 14-day civil windows; field-specific `assessedDays` in each; numbers vs direction separately qualified; no similar/improving/hormonal/AI. Android shots `qa/cycle-phase30-exposure-comparison/`. Contract: `docs/CYCLE_OBSERVATION_EXPOSURE_COMPARISON_CONTRACT.md`. Previous freeze: **52.0.0**. **CYCLE PHASE 29 — EXPOSURE-AWARE OBSERVATION RATES FINAL-FROZEN.** First allowed denominator: field-specific `assessedDays` (PRESENT + explicit ABSENT). Optional `exposure` on Pregnancy/Peri Journal expanded rows only. Thresholds: present≥2, assessed≥5, coverage≥30%. No calendar/log/mode-day denominators, directional claims, diagnosis, or OpenRouter. Android shots `qa/cycle-phase29-exposure-rates/`. Contract: `docs/CYCLE_OBSERVATION_EXPOSURE_RATES_CONTRACT.md`. Previous freeze: **51.0.0**. **CYCLE PHASE 28 — EXPLICIT OBSERVATION ASSESSMENT & EXPOSURE FOUNDATION FINAL-FROZEN.** Three-state UNKNOWN / ABSENT / PRESENT on `CycleLog.observationAssessments` (explicit ABSENT only; no new table). Registry `assessmentEligible` defaults false. V1: Pregnancy `nausea`/`vomiting`/`fatigue`; Peri `hot_flashes`/`night_sweats`/`fatigue`. No percentages, diagnosis, or OpenRouter. Android shots `qa/cycle-phase28-observation-assessment/`. Contract: `docs/CYCLE_OBSERVATION_ASSESSMENT_CONTRACT.md`. Previous freeze: **50.0.0**. **CYCLE PHASE 27 — MODE CAPABILITY & PRESENTATION INTEGRITY FINAL-FROZEN.** Canonical presentation matrix in `server/src/lib/cycleModeCapabilityMatrix.js` + `mobile/src/lib/cycleModeCapabilityMatrix.js` (duplicated; Metro must not import repo-root). Contract: `docs/CYCLE_MODE_CAPABILITIES_CONTRACT.md`. Android shots `qa/cycle-phase27-mode-capabilities/`. Previous freeze: **49.0.2**. **CYCLE PHASE 26 — DENOMINATOR-SAFE PERIMENOPAUSE OBSERVATION SUMMARIES FINAL-FROZEN.** Journal-only `perimenopause.observationSummaries` on `GET /api/cycle` when mode is `PERIMENOPAUSE`. Last 30 civil days, 2+ occurrences, no percentages or denominators. No episode model. Android shots `qa/cycle-phase26-peri-observation-summaries/`. Previous freeze: **48.0.0**. **CYCLE PHASE 25 — DOCTOR SUMMARY PERIMENOPAUSE TRACKING CONTEXT FINAL-FROZEN.** Additive `perimenopauseContext` on `GET /api/cycle/doctor-summary` when mode is `PERIMENOPAUSE` (user-selected tracking, not a diagnosis). Android shots `qa/cycle-phase25-doctor-perimenopause/`. Previous freeze: **47.0.0**. **CYCLE PHASE 24 — PERIMENOPAUSE MODE FOUNDATION FINAL-FROZEN.** Explicit Settings entry only; no age/symptom auto-entry; no menopause confirmation. `GET /api/cycle` → `perimenopause` read model (variability from last 6 unclamped intervals). Contract: `docs/CYCLE_PERIMENOPAUSE_CONTRACT.md`. Android shots `qa/cycle-phase24-perimenopause/`. Previous freeze: **46.0.0**. **CYCLE PHASE 23 — DENOMINATOR-SAFE PREGNANCY OBSERVATION TRENDS FINAL-FROZEN.** Journal-only factual summaries on `GET /api/cycle/pregnancy` → `observationTrends` (current ACTIVE episode ∩ last 30 days; lower bound = `startedAt`; 90-day query cap). No percentages, calendar denominators, increasing/worsening, or trimester frequency. Registry `pregnancyTrendEligible` defaults false. Contract: `docs/PREGNANCY_OBSERVATION_TRENDS_CONTRACT.md`. Android shots `qa/cycle-phase23-pregnancy-observation-trends/`. Previous freeze: **45.0.0** (**CYCLE PHASE 22 — PREGNANCY SYMPTOM & BODY CHANGES EXPERIENCE FINAL-FROZEN.** Canonical CycleLog only (no PregnancyLog table). `GET /api/cycle/pregnancy` adds episode-scoped `todayObservations` / `recentObservations` (approved keys; unknown hidden). New registry key `heartburn` defaults AI/partner DENY, doctor summary EXCLUDE. Contract: `docs/PREGNANCY_OBSERVATION_CONTRACT.md`. Android shots `qa/cycle-phase22-pregnancy-observations/`. Previous freeze: **44.0.0** (**CYCLE PHASE 21 — PREGNANCY TIMELINE & MILESTONES FINAL-FROZEN.** `/cycle/pregnancy/timeline` + compact Overview/Journal CTA. Server `timeline` on `GET /api/cycle/pregnancy` consumes Phase 18 week/day/trimester (no client dating math, no fruit in rows, not partner/AI/doctor-summary). Contracts: `docs/PREGNANCY_TIMELINE_CONTRACT.md`, `docs/PREGNANCY_TIMELINE_DATA.md`. Android shots `qa/cycle-phase21-pregnancy-timeline/`. Previous freeze: **43.0.0** (**CYCLE PHASE 20 — DOCTOR SUMMARY PREGNANCY HEADER FINAL-FROZEN.** `/cycle/summary` + `GET /api/cycle/doctor-summary` may include `pregnancyContext` when mode is `PREGNANCY` and an ACTIVE episode exists (server-owned dating; no fruit; not partner/AI). Artifacts `qa/cycle-phase20-doctor-pregnancy/`. Previous freeze: **42.0.0** (**CYCLE PHASE 19 — PREGNANCY BABY SIZE & WEEK-BY-WEEK VISUAL GUIDE FINAL-FROZEN**). Static Hadlock/WHO/Williams catalog in `mobile/src/lib/pregnancyWeekData.js`; `GET /api/cycle/pregnancy` attaches `weekDevelopment` from the Phase 18 week (no client math, no engine change). Fruit stays owner UI only. Contracts: `docs/PREGNANCY_BABY_SIZE_CONTRACT.md`, `docs/PREGNANCY_BABY_SIZE_DATA.md`. Android shots `qa/cycle-phase19-baby-size/`. Previous freeze: **41.0.0** (**CYCLE PHASE 18 — PREGNANCY MODE FOUNDATION FINAL-FROZEN**). `PREGNANCY` is a live explicit-entry mode with `CyclePregnancyEpisode` (one ACTIVE). Forecast engine unchanged. Contract: `docs/CYCLE_PREGNANCY_CONTRACT.md`. Android shots `qa/cycle-phase18-pregnancy/`. Previous freeze: **40.0.1** (**CYCLE PHASE 17 — TTC COLD-START RELIABILITY**; Phase 16 TTC product remains frozen). `TRY_TO_CONCEIVE` is a live presentation mode. `GET /api/cycle/ttc` is a 180-day owner read model gated on auth hydration. Contract: `docs/CYCLE_TTC_CONTRACT.md`. Android shots `qa/cycle-phase17-ttc-coldstart/`. OpenRouter Cycle AI remains out of scope. Phase 11: no client LMP phase engine; `observationInsights` no longer emits estimated-phase pain lines; ICS/BBT reminders honor LIMITED; GET `/api/cycle` is `private, no-store`; JSON export + confirmed Cycle wipe (`POST /api/cycle/wipe`) destroy server rows and local offline queue. Live Neon has Cycle Phase 7–11 columns (including `CycleLog.observations` JSONB + `observationSchemaVersion`) but no `_prisma_migrations` table — keep additive `prisma db execute` (Phase 8 pattern); do not fabricate migrate history. Phase 11 tracking foundation is **final-frozen** after Phase 12 Android QA (`39.0.26`; artifacts `qa/cycle-phase12-rich-tracking/`). Phase 13 Journal observation trends are **final-frozen** (`39.0.27`; artifacts `qa/cycle-phase13-observation-trends/`). Dedicated `GET /api/cycle/doctor-summary` + `buildCycleDoctorSummaryData`. Default-deny; fertility/sexual/notes are document-scoped opt-in. Not partner, AI, or personal-export. Phase 15 clinician locale rendering is **final-frozen** (`39.0.29`; artifacts `qa/cycle-phase15-locale/`). One HTML renderer + Quest-style `{ka,en,fr,ru}` catalog. Document-scoped language chips on `/cycle/summary` (default ka, no persist, does not change app language). Server stays language-neutral. iOS Cycle QA still deferred. Phase 10 analytics remain server-owned. Phase 9/11/13 observations do not change the engine.
- Cycle engine is `server/src/lib/cycle.js` only. Period ranges are inferred from consecutive `CycleLog` days with `flow` `light|medium|heavy`. Spotting is not a period day. No `Period` table. `PUT /api/cycle/period` is a shortcut (`start` / `end` / `fill`) over CycleLog upserts; unique `(userId, date)` is the duplicate guard. **End Period does not invent flow** — it clears bleed from the chosen day onward (that day is no longer a period day). `fill` is explicit + confirmed. Schema has no “bled, intensity unknown”. Phase 6/6.1: last successful `CycleBundle` is cached per `userId`; pending mutations persist encrypted (AES-256-GCM + SecureStore DEK) on native until sync. Web is session-only. Client must not recompute cycle day / phase / windows. Partner peek is online-only. Logout keeps scoped data; never replay User A into User B. Phase 7: `CycleLog.ovulationTest` / `pregnancyTest` are nullable user-logged enums (`negative|positive|unclear`). They do not change ovulation/fertile-window math or auto-switch Pregnancy mode. Partner share does not include OPK, pregnancy tests, BBT, mucus, or intercourse. Mobile **15.0.0**.
- Cycle AI tips: `POST /api/cycle/insights` via EvidenceMD (`CYCLE_WELLNESS`), cached ~18h on `CycleProfile.aiInsights`; local Flo-like tips as instant fallback
- Medi Quest Phase 3 is live on **27.0.0**. Home module + `/medi-quest` hub, claim/XP/coins/level-up, Socket.IO `user:${userId}` rooms on the existing admin server. Hydration retries use `HydrationIntakeEvent` (unique `clientEventId`); native snapshots MAX. Step quests assign only when `StepTrackingCapability.status === AVAILABLE` (UNKNOWN = no steps quest). Hydration goal: local settings win pre-launch; PUT when server is null or differs. Socket events are UX only — always reconcile with `GET /api/quests`. Do not start achievements / Rewards Store / social. Economy unchanged. After schema change: stop API → `npx prisma generate` → `prisma db push` → `npm run seed`.
- If `npx prisma generate` fails with EPERM on Windows, stop the API server (DLL lock), then regenerate
- Default seed login: `ADMIN_EMAIL` / `ADMIN_PASSWORD` (see `server/.env.example`)
- Packages: FREE (3/day), STANDARD (50/day), ULTIMATE (unlimited). Unused credits reset at Tbilisi midnight; hitting 0/N starts a 24h lock from that moment, then a refill ping (quiet hours → 08:15).
- App settings: maintenance/offline mode, min version, force update, registrations gate, **QA OTP** (0000 phone / 000000 email). Settings flag may be on in non-production. **`NODE_ENV=production` never accepts the master OTP** (RA-01). `QA_OTP_CODE` env also enables it outside production. Turn the settings flag off after testers finish.
