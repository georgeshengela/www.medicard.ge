# Remaining TypeScript errors (baseline, not RA-01)

RA-00 counted **123** `tsc` errors.

RA-01 did **not** mass-fix them.

Touched RA-01 mobile files: `AuthContext.tsx`, `app/_layout.tsx`, `sign-in.tsx`, `record/[id].tsx`, `appVersion.ts`, `medicardVersion.js`.

Do not treat the repo as typecheck-clean. Tracked as **RA00-007** for a later phase.
