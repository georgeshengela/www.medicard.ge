# 2026-08-29

Home hub section standard (locked in): **title outside the card, then the section**. Same as შემდეგი მიღება. Weight = წონის კონტროლი, steps = აქტიურობა. Use `HomeSectionTitle`. Never put the section name inside the card.

AI quota: countdown starts when the daily cap is **hit**, not at month end. FREE = 3/day, lock is `now + 24h` (`PeriodUsage.resetAt`). Copy is "გამოიყენეთ 3 შეკითხვა 3-დან" + "განახლდება ხვალ, HH:mm-ზე" — never `3/3 · 0/3 შეკითხვა`.

Steps-goal +3 never hit `User.points`: the celebration badge is local, `POST /api/check-in/steps-goal` is not on production (`medicard.ge` 404), and the app swallowed the error then cleared the goal. Profile reads the server ledger (daily check-in 5 only). Credited the two missed +3s. Client now queues awards and retries on profile / hydrate. Future completions still need that route deployed.

Profile-setup results is live now: force re-analyze uses profile + extras + 14-day metrics + scheduled meds. Score moves with the data. Results page has ხელახალი ანალიზი. Expo still points at `https://medicard.ge` — server changes need a deploy (or `EXPO_PUBLIC_API_URL` to localhost) before localhost:8081 hits the new logic.

Hydration module shipped locally: home card under steps (`ჰიდრატაცია` title above card), hub at `/health-metrics/hydration` with glass, calendar week, log, history, details, level. Daily total syncs to `hydrationMl`. Default goal 2000ml.

Hydration 1d hub (Figma 9017:196580): fixed column — app bar + hero + glass `flex:1` + pinned tabs. No page scroll on 1d. Tabs are hydration-only (`#F3F4F6`, h 40, radius 14, 14/20 semibold). Target/remaining use `2,000ml`. Remaining copy: `უნდა დალიოთ კიდევ ${ml}!`. StepsPeriodTabs left untouched.

Hydration 1w calendar (Figma 8851:187189): each cell is date + 32px glyph. Teal-page check is Figma `check-circle` `#22C55E` evenodd. Miss is `close-x-circle` `#F43F5E`. Empty ring `#4B5563` / out-of-month `#D1D5DB`. Miss = in-month past day that did not hit the goal (including 0ml). Today is `#F0FDFA` + `#14B8A6` tile.

Symptoms intro reel (10.0.11): woman dunk → knee bones/veins, woman sprint → abdomen, solo man heavy-bag training → real shoulder grimace (CGI bone window cut). Full-bleed cover, muted loop.

Medi / analysis now load `withPatientAiContext`: health profile + last 14 `HealthMetricDaily` rows (steps, hydrationMl, weight, sleep, HR, BP, nutrition, active minutes, distance) + scheduled meds + cycle mode. Doctor, consilium, image, skincare, med review, symptom check, cycle insights, and onboarding re-analyze. Needs a **server deploy** before medicard.ge sees it. Step goals stay local-only (daily steps still go to AI).

Hydration sync must send a **delta** (new glass / negative on delete). Server `hydrationMl` merge is additive; sending the day total double-counts. Server now accepts negative deltas and clamps the stored day to 0–20,000.

60s Medicard social commercial v3: same girl + same feature script, more emotion, she HOLDS the iPhone. Real screens keyed onto the green phone glass (thumb stays), swipe/scroll between home→steps→hydro→anatomy→cycle→Consilium→meds. Hosted: `https://d2ol7oe51mr4n9.cloudfront.net/user_3G361cCmsCaoLFMqO6NmaDCtwFR/74c05fc1-2470-4774-8fd0-e752e0bf72f0.mp4`.
