# 2026-09-16

- TestFlight 1.8.62: Settings → MEDICARD AI only showed Apple Intelligence & Siri, Search, Mobile Data. That is the default iOS app page when **no privacy permission has ever been requested**. The in-app card sent the owner to Settings after `getPermissionsAsync` + Modal + `setBusy` ate the tap, so iOS 26 never showed Allow and never created a Notifications (or Health) row.
- Fix `1.0.0.8.63` / iOS `1.8.63`: `requestPermissionsAsync` / HealthKit `requestAuthorization` is the first await on **ნებართვის მიცემა**. No Modal, no Open Settings. Overlay card is scrollable. `UIBackgroundModes` remote-notification. Need a new EAS iOS build — 1.8.62 cannot pick this up.
- Android full audit on Windows Pixel_8 API 35: native debug APK 1.0.0.8.63 + Metro JS 1.0.0.8.64. Female UI user completed OTP 0000 and cycle hub. Written QA reports were later deleted on owner request.
