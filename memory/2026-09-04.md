Landing now self-hosts official FiraGO WEB (Regular–ExtraBold) from bBoxType, not Fontsource. Fontsource/Google cuts are Latin-only and drop Georgian `case`. Body stays Mkhedruli. Titles, brand, kickers use `font-feature-settings: "case"` → Mtavruli at Regular 400 (800 and 600 were too heavy).

Landing wrap is back to 1504px. First FitScan pass was too empty; filled with original 15 feature cards, day strip, ticker, 4 stats, chips. Still mixed FitScan bands + Medicard colors + FiraGO case titles at 400. Not pushed.

User called out empty sections. Cause: `.reveal { opacity: 0 }` hid cards until scroll, plus hollow FitScan shells (`#why`, `#when`, `#medi` split, compose 2×2+phone, extra 12+ proof card, hero trust) sitting on huge min-heights. Cut those, show content immediately, keep 1504 + FiraGO case 400 + 15-card encyclopedia. Not pushed.

Polish pass: same content, new components. Hero orbit + day dock (active teal). Proof as one dark 4-col board + dark stores + teal QR. Features hover invert, Medi rail, 12+ mark. How as one navy stepper with icons. Plans checks + featured teal edge. FAQ +/–. CTA split with QR/stores. Disclaimer in footer. No gradients. Still local only.

Official slogan: შენი ჯანმრთელობა. შენი ისტორია. შენთან ყოველთვის. Hero only, DM Davit Guramishvili UNI (CC BY-SA 3.0). Teal download band has no slogan — kicker + ჩამოტვირთე ტელეფონზე, QR, real App Store / Play badges (მალე). Rest of the site stays FiraGO. Not in the Expo app yet.

Privacy page at /privacy now matches landing chrome. Terms page at /terms is the same layout with the official 52-section Georgian terms. Sources: scripts/privacy-source.md, scripts/terms-source.md. Rebuild: node scripts/build-legal-pages.mjs. Dates 4 სექტემბერი, 2026. Legal entity ID/street still missing — Medicard.GE + support@medicard.ge only. Mobile terms date synced.

Home წონის კონტროლი card stays the same Nightingale size: kg + BMI index, WHO 4-zone bar, category / healthy range / week delta. Title stays above. App 20.0.18.

Decode-lab chat UI rebuilt in **19.0.1**: live scan theater while pages upload, camera stays after the first shot plus “კიდევ გადავიღო?”, sticky analyze dock. Lab tracking shipped as **19.0.0**. გაშიფრე ანალიზები uploads multiple images + waiting animation. If the sheet has no date, the date sheet asks. Hub `/lab`, date page matches Figma 8852:143210 (badges ნორმა / მაღალი / დაბალი), parameter tap opens the chart. Home **ლაბორატორია** title stays above the card.

Lab pipeline **20.0.1**: 4 photos of one day’s test used to fire 4 EvidenceMD calls. Extract reads one page at a time through OpenRouter (same single-file path that already worked), first page costs 1 credit, extra pages append. EvidenceMD runs only if the user taps გაანალიზე Medi-სთან (`POST /api/ai/explain-lab`). Imaging/skin unchanged. 20.0.0 one-shot multi-image hang left the studio on იკითხება.

Lab param chart **20.0.3**: rebuilt from Figma `11363:109238` (Nightingale range). Teal ნორმა badge + check, violet flask, big range/value, 1დ/1კ/1თ/1წ/ყველა tabs, dashed 6×3 grid, `#8B5CF6` rounded pills, dark tooltip. Not a circle — that node is the floating-bar range chart.

Lab plot **20.0.5**: Figma `9091:158009` dual teal/mint linear area chart, plus a teal **ნორმა** lane (band + badge). Parameter title has Latin abbrev + info icon; sheet explains the analyte in Georgian (catalog + generic fallback). App **20.0.5**.

Profile avatar is a concentric circle (20.0.15). გარეგნობა theme control is a compact pill with light/dark/system preview swatches — no duplicate title or hint (20.0.16). აპის გახსნა is its own female-only section with a short explainer (20.0.17).

Onboarding privacy page now shows the full medicard.ge/privacy text (synced from scripts/privacy-source.md). Decline CTA is არ ვეთანხმები. App **20.0.28**.

Profile-setup last screen is no longer Asklepios score generation. Analyzing uses Figma `8846:211832` teal gradient + three `#5EEAD4` 48px rings, avatar + logo + `{name} ვამზადებთ პროფილს`, then home. Results step skipped. App **20.0.29**.

New accounts were seeing the previous user's drinks and lab panels because those stores were device-global. Hydration, lab, dose logs, skincare, and symptom history are now keyed by userId; leftover unscoped caches are wiped on login. Assessment `waterIntakeL` is no longer shown as today's logged water. App **20.0.31**.

Login streak week no longer paints X on days before the account existed. New register today = empty dots Mon–before-join, check only today. Missed days after join still skip. App **20.0.32**.

Profile-setup resume no longer dumps back to avatar after Face ID. `nextProfileSetupHref` continues at the unfinished step; Face ID skip/enable is saved. App **20.0.27**.

Conditions step is now a medication-style search: 60+ catalog items, type-your-own add, common chips. App **20.0.26**.

Mood carousel faces are 10% smaller; extra lane height so the smile/chin is not clipped. App **20.0.25**.

Smoking step copy: დიახ, ვეწევი / ყოველდღიურად ვეწევი (not ვეწევ). App **20.0.24**.

Assessment dark pass **20.0.23**: blood type and the rest of the questionnaire were still painting light mint/gray (`#F0FDFA`, `lightColors.primary100` = dark green). Shared tokens now include `tint` / `tintStrong` / `tintBorder` / `hairline` / `rowLine` / `brand` / `brandInk`. Dark: tint `#042F2E`, brand ink `#99F6E4`, hairlines `#374151`.

QA OTP: admin Settings toggle or `QA_OTP_CODE=0000`. Phone 0000 / email 000000 always verify while on. Off by default. Needs server deploy + `db push` for `qaOtpEnabled`.

Phone numbers are unique: linking a number already on another account returns `PHONE_TAKEN` and the app shows an alert. App **20.0.33**.
