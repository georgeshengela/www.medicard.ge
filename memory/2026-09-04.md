Landing now self-hosts official FiraGO WEB (Regular–ExtraBold) from bBoxType, not Fontsource. Fontsource/Google cuts are Latin-only and drop Georgian `case`. Body stays Mkhedruli. Titles, brand, kickers use `font-feature-settings: "case"` → Mtavruli at Regular 400 (800 and 600 were too heavy).

Landing wrap is back to 1504px. First FitScan pass was too empty; filled with original 15 feature cards, day strip, ticker, 4 stats, chips. Still mixed FitScan bands + Medicard colors + FiraGO case titles at 400. Not pushed.

User called out empty sections. Cause: `.reveal { opacity: 0 }` hid cards until scroll, plus hollow FitScan shells (`#why`, `#when`, `#medi` split, compose 2×2+phone, extra 12+ proof card, hero trust) sitting on huge min-heights. Cut those, show content immediately, keep 1504 + FiraGO case 400 + 15-card encyclopedia. Not pushed.

Polish pass: same content, new components. Hero orbit + day dock (active teal). Proof as one dark 4-col board + dark stores + teal QR. Features hover invert, Medi rail, 12+ mark. How as one navy stepper with icons. Plans checks + featured teal edge. FAQ +/–. CTA split with QR/stores. Disclaimer in footer. No gradients. Still local only.

Official slogan: შენი ჯანმრთელობა. შენი ისტორია. შენთან ყოველთვის. Hero only, DM Davit Guramishvili UNI (CC BY-SA 3.0). Teal download band has no slogan — kicker + ჩამოტვირთე ტელეფონზე, QR, real App Store / Play badges (მალე). Rest of the site stays FiraGO. Not in the Expo app yet.

Privacy page at /privacy now matches landing chrome. Terms page at /terms is the same layout with the official 52-section Georgian terms. Sources: scripts/privacy-source.md, scripts/terms-source.md. Rebuild: node scripts/build-legal-pages.mjs. Dates 4 სექტემბერი, 2026. Legal entity ID/street still missing — Medicard.GE + support@medicard.ge only. Mobile terms date synced.

Home ჰიდრატაცია card is Figma 9283:202564 — timestamp + drop, big ml, red-dot goal, hatched teal bar. Title stays above the card. App 18.0.12.

Decode-lab chat UI rebuilt in **19.0.1**: live scan theater while pages upload, camera stays after the first shot plus “კიდევ გადავიღო?”, sticky analyze dock. Lab tracking shipped as **19.0.0**. გაშიფრე ანალიზები uploads multiple images + waiting animation. If the sheet has no date, the date sheet asks. Hub `/lab`, date page matches Figma 8852:143210 (badges ნორმა / მაღალი / დაბალი), parameter tap opens the chart. Home **ლაბორატორია** title stays above the card.

Lab pipeline **20.0.0**: 4 photos of one day’s test used to fire 4 EvidenceMD calls. Now one OpenRouter extract (`POST /api/ai/extract-lab`) reads all pages + printed ranges, stores the test in `/lab`, 1 credit. EvidenceMD runs only if the user taps გაანალიზე Medi-სთან (`POST /api/ai/explain-lab`). Imaging/skin unchanged. Needs a server deploy before Expo Go against medicard.ge can use the new routes.
