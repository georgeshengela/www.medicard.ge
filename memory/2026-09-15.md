# 2026-09-15

- Limited beta execution: identified owner-tested EAS `1.0.0.8.41` build/versionCode 2 (`f3c4a51`) as AdHoc IPA (1 device) + APK. All EAS history is preview/internal. `eas.json` `submit.beta` added (Play internal/draft, iOS team 7699S4PS73). Submit of those artifacts failed: iOS needs `ascAppId` or interactive Apple login (session expired); Android has no Google service account on EAS. Play listing `ge.medicard.app` 404; iTunes lookup 0. Did not start `eas build --profile production` (paid). No tester link. Tree candidate remains `1.0.0.8.56`.
- Release package (not store submit): purchase CTAs default off; AiInteraction dry-run retention script; S3/R2 env contract; seed admin policy unit-tested with and without ADMIN_PASSWORD; local `/api/app/status` `consumerPurchasesEnabled: false`. 40 focused tests passed. Individual Apple/Play health-app eligibility still blocks submit. Public identity `1.0.0.8.56`. No deploy/SQL/EAS.
- Full-app release audit: `docs/RELEASE_READINESS.md` **NO-GO** for public store. Local crash/AI/HealthKit-string fixes; no deploy, SQL, EAS, or submit. Live `https://medicard.ge` `/uploads` 401, Pets 401 unauthenticated.
- Hydration Home card now reads stored `HealthMetricDaily.hydrationMl` so Expo matches water logged in the installed app. Public identity `1.0.0.8.52`.
- Home activity card: header is the only `<button>`; chart day hits stay siblings so web no longer nests buttons. Public identity `1.0.0.8.50`.
- Progress rings no longer use SVG `rotation`/`origin` (web mapped that to `transform-origin` and React warned). Pet photo ring plus other rings now use `transform="rotate(-90 cx cy)"`. Public identity `1.0.0.8.48`.
- Pets hub ვეტკლინიკები: removed Dogdog hours blurb; list is bundled so production `/api/pets/clinics` UUID 400 no longer blanks the section. Public identity `1.0.0.8.43`.
- Admin V3: every page body is full workspace width (`--v3-page-max: none`). Settings/forms no longer sit on 720/920/1440 rails. Cache `admin-v3.css` / `unify.css` / `modules.css` `v3.101`, `admin-v25.css` `ops24`. Login cards, drawers, dialogs stay constrained.
- Pets manage hub `/pets`: swipe right edit, swipe left archive (confirm sheet, not hard delete). Profile **ყველას ნახვა** always opens this page. Below the list: add pet + products / მოვლა / Medi Vet / წონა for the featured pet. Not on Home. Public identity `1.0.0.8.39`.
- Profile ჩემი ცხოველები restyled with teal weather-card styling (teal wash, photo halo, Medi Vet + მოვლა chips, filled empty CTA). Title stays above the card. Not on Home. Public identity `1.0.0.8.37`.
- App showed ლიეჟი after a location toggle, admin stayed საქართველო: profile `extraAnswers.location` can keep a live client reverse-geocode while admin map/user pages read `UserLocation.countryCode` (grant-only home place). Grant Tbilisi→Liège was dropped as an implausible 200km/15min jump, so the table never got BE. Jump guard is now Georgia snapback only (Liège→Tbilisi still blocked). Needs server deploy + re-grant to rewrite the stored country. No version bump.
- Pets UI restyle: dropped the 4–5 step care/record wizards. Forms now use Medicard `Input` / `DateField` / `SegmentedField` / `ChoiceTiles` / `Card` / sticky save footer. `ka.pets.vetDoctor` labels the clinic doctor (was colliding with Medi Vet). Public identity `1.0.0.8.26`. No Neon/deploy/push.
- Add pet is a full page (not a sheet). დაახლოებით uses calendar year + Georgian month names (`იანვარი`…). Name placeholder `მაგ. მაქსი`. Version `1.0.0.8.28`.
- Pet weight / allergy / condition forms now use the same Figma InputFieldBase, DateField, and PetFilterChip language as add-pet. Lists and pet-profile summaries got icon wells. Public identity `1.0.0.8.29`.
- მოვლა hub now has an obvious Plus CTA (`მოვლის დამატება`) in the empty state, header, and profile card (empty tap goes to plan). Care forms use Figma fields + PetFilterChip instead of ChoiceTiles. Public identity `1.0.0.8.30`.
- მოვლის დამატება is a choice page again (plan / product / past dose) so products is not skipped. Public identity `1.0.0.8.31`.
- Pet weight save rejected today's date before UTC midnight (`parseBirthDate` UTC future check). Measurement dates now use local civil day (`parseCivilDate`). Today is allowed.
- Medi Vet chat: 16px message gap (was stacked). Pet weight history uses Figma 9224:157219 dual-area chart + 48px mint score tile; latest card uses WeightCircleProgress. No Nightingale copy.
- Pets release readiness (no SQL/deploy/push/version bump): owner dog/weight/allergies/Medi Vet chat accepted. Phone Metro `:8081` → production `https://medicard.ge`. Hosted Neon already has 11 `Pet*` tables. Leftover `:4010` is not that session. OS reminders still unverified. See `docs/PETS_RELEASE_READINESS.md`.
