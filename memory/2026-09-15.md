# 2026-09-15

- Limited beta execution: identified owner-tested EAS `1.0.0.8.41` build/versionCode 2 (`f3c4a51`) as AdHoc IPA (1 device) + APK. All EAS history is preview/internal. `eas.json` `submit.beta` added (Play internal/draft, iOS team 7699S4PS73). Submit of those artifacts failed: iOS needs `ascAppId` or interactive Apple login (session expired); Android has no Google service account on EAS. Play listing `ge.medicard.app` 404; iTunes lookup 0. Did not start `eas build --profile production` (paid). No tester link. Tree candidate remains `1.0.0.8.56`.
- Release package (not store submit): purchase CTAs default off; AiInteraction dry-run retention script; S3/R2 env contract; Tbilisi cron YAML prepared not registered; seed admin policy unit-tested with and without ADMIN_PASSWORD; local `/api/app/status` `consumerPurchasesEnabled: false`. 40 focused tests passed. Individual Apple/Play health-app eligibility still blocks submit. Public identity `1.0.0.8.56`. No deploy/SQL/EAS.
- Full-app release audit: `docs/RELEASE_READINESS.md` **NO-GO** for public store. Local crash/AI/HealthKit-string fixes; no deploy, SQL, EAS, or submit. Tbilisi finalizer still not scheduled. Live `https://medicard.ge` `/uploads` 401, Tbilisi flags on + `pilotMode`, Pets 401 unauthenticated.
- District hub ring is now your credited steps toward the daily cap (fills and counts up), not a 1% district-goal score. District target stays as the line under the ring. Public identity `1.0.0.8.56`.
- Hydration Home card now reads stored `HealthMetricDaily.hydrationMl` so Expo matches water logged in the installed app. District hub listens to `health:metrics` / `tbilisi-moves:update` on the user socket and refreshes without a tap. Public identity `1.0.0.8.52`.
- District war now copies stored Home steps (`HealthMetricDaily`) onto the competition credit. Native rebuild still pushes Health → `/api/health-metrics/sync`; Expo Go `GET /me` shows the same number. No Health Connect permission sheet on retry. Public identity `1.0.0.8.51`.
- Home activity card: header is the only `<button>`; chart day hits stay siblings so web no longer nests buttons. Public identity `1.0.0.8.50`.
- Progress rings no longer use SVG `rotation`/`origin` (web mapped that to `transform-origin` and React warned). Pet photo ring plus other rings now use `transform="rotate(-90 cx cy)"`. Public identity `1.0.0.8.48`.
- District war now credits the same Health Connect / HealthKit Steps read as Home. Retry no longer opens the OS permission sheet (that cancelled the read and kept 0). Native hub also pushes those samples to `/api/health-metrics/sync` so Expo sees the same daily total. Still not copying `HealthMetricDaily` into the competition ledger. Expo Go still cannot PUT. Public identity `1.0.0.8.46`.
- Pets hub ვეტკლინიკები: removed Dogdog hours blurb; list is bundled so production `/api/pets/clinics` UUID 400 no longer blanks the section. Public identity `1.0.0.8.43`.
- Admin V3: every page body is full workspace width (`--v3-page-max: none`). Settings/forms no longer sit on 720/920/1440 rails. Cache `admin-v3.css` / `unify.css` / `modules.css` `v3.101`, `admin-v25.css` `ops24`. Login cards, drawers, dialogs stay constrained.
- District ingest now uses the same Health Connect `after` read as Home when `between` drops day-buckets (end after now). First unsynced native hub focus prompts Steps. Expo Go still cannot PUT. Public identity `1.0.0.8.41`.
- Home 583 vs district 0: personal `HealthMetricDaily` is not the competition ledger. Hub now labels that split. Native Health Connect ingest tries the same read-then-grant path as Home (no false 0 from a bad origin aggregate). Expo Go still cannot PUT observations. Public identity `1.0.0.8.40`.
- Pets manage hub `/pets`: swipe right edit, swipe left archive (confirm sheet, not hard delete). Profile **ყველას ნახვა** always opens this page. Below the list: add pet + products / მოვლა / Medi Vet / წონა for the featured pet. Not on Home. Public identity `1.0.0.8.39`.
- Preview native still showed district 0 while Home steps updated: Home is `HealthMetricDaily`/`StepLog` (not competition). Hub now awaits HealthKit/Health Connect ingest before painting the board. Permission sheet still only on enroll/retry. Public identity `1.0.0.8.38`.
- Profile ჩემი ცხოველები restyled to match Tbilisi Moves / weather cards (teal wash, photo halo, Medi Vet + მოვლა chips, filled empty CTA). Title stays above the card. Not on Home. Public identity `1.0.0.8.37`.
- Profile თბილისი მოძრაობს: live district card (goal ring, rank badge, steps/rank chips, join CTA). Title stays above the card. Not on Home. Public identity `1.0.0.8.36`.
- თბილისი მოძრაობს დღის რეიტინგი: gold/silver/bronze podium for top 3, medal badges, district % rows, people step rows, “შენი ადგილი” dock. Ranking stays activity (goal %), not medical. Public identity `1.0.0.8.35`.
- თბილისი მოძრაობს hub: rebuilt score ring with Cycle-style arc paths (no SVG rotation/origin), centered disc, no colliding labels. Removed Expo Go sync alert. Public identity `1.0.0.8.34`.
- თბილისი მოძრაობს mobile hub restyle: custom chrome (no system header), Figma ring, district card, stat tiles, action chips, district/people boards. Same chrome on enroll/membership/history/awards. Copy stays activity ranking, not medical. Public identity `1.0.0.8.33`.
- თბილისი მოძრაობს: Figma `8915:67254` score ring is on the **mobile hub**, not admin. Admin `#/tbilisi-moves` now matches V3 settings (toolbar, icon KPIs, toggles, sticky save) with Socket.IO `tbilisi-moves:live`. Public identity `1.0.0.8.32`. No Neon/deploy/finalize.
- თბილისი მოძრაობს live activation on hosted Neon (`neondb` eu-central-1): applied `tbilisi-moves-phase2.sql` then `phase4.sql`. Admin PATCH enabled `featureEnabled` + `enrollmentOpen`; `pilotMode` stayed true. Live status on `https://medicard.ge` is schemaReady/feature/enrollment on. Expo Go hub/enroll no longer waits 12s for sensors. Metro `:8081` not retargeted. No fake scores, no round finalize, no awards. Documented QA email `cycle.qa.phase6@medicard.ge` is **absent** on hosted Neon so enroll write-smoke was not run (did not create a user).
- თბილისი მოძრაობს: live Expo Go Home 1,000+ steps are personal `HealthMetricDaily`/`StepLog` via `GET /api/health-metrics` (device-local day, `source: 'merged'`), not HealthKit/Pedometer. Cannot feed competition (no Tbilisi interval, no origin split, manual indistinguishable). `__DEV__` `[steps-origin]` log added. Enroll wait capped 12s; OS permission only on enroll/retry. Native ingest still pending a development build. Live `:8081` → `https://medicard.ge` not retargeted. No Neon/flags/scheduler/version bump.
- თბილისი მოძრაობს Phase 6 continuation: live phone is Expo Go `192.168.1.187` → Metro `:8081` → `https://medicard.ge` (not retargeted). Enroll awaits same-day Tbilisi sensor PUT (12s cap); hub has retry; permission sheets only on enroll/retry. Native provider read **not** executed (`adb` empty, no `mobile/ios`). No Neon/flags/scheduler/version bump. `pilotMode` true.
- App showed ლიეჟი after a location toggle, admin stayed საქართველო: profile `extraAnswers.location` can keep a live client reverse-geocode while admin map/user pages read `UserLocation.countryCode` (grant-only home place). Grant Tbilisi→Liège was dropped as an implausible 200km/15min jump, so the table never got BE. Jump guard is now Georgia snapback only (Liège→Tbilisi still blocked). Needs server deploy + re-grant to rewrite the stored country. No version bump.
- Pets UI restyle: dropped the 4–5 step care/record wizards. Forms now use Medicard `Input` / `DateField` / `SegmentedField` / `ChoiceTiles` / `Card` / sticky save footer. `ka.pets.vetDoctor` labels the clinic doctor (was colliding with Medi Vet). Public identity `1.0.0.8.26`. No Neon/deploy/push.
- Add pet is a full page (not a sheet). დაახლოებით uses calendar year + Georgian month names (`იანვარი`…). Name placeholder `მაგ. მაქსი`. Version `1.0.0.8.28`.
- Pet weight / allergy / condition forms now use the same Figma InputFieldBase, DateField, and PetFilterChip language as add-pet. Lists and pet-profile summaries got icon wells. Public identity `1.0.0.8.29`.
- მოვლა hub now has an obvious Plus CTA (`მოვლის დამატება`) in the empty state, header, and profile card (empty tap goes to plan). Care forms use Figma fields + PetFilterChip instead of ChoiceTiles. Public identity `1.0.0.8.30`.
- მოვლის დამატება is a choice page again (plan / product / past dose) so products is not skipped. Public identity `1.0.0.8.31`.
- Pet weight save rejected today's date before UTC midnight (`parseBirthDate` UTC future check). Measurement dates now use local civil day (`parseCivilDate`). Today is allowed.
- Medi Vet chat: 16px message gap (was stacked). Pet weight history uses Figma 9224:157219 dual-area chart + 48px mint score tile; latest card uses WeightCircleProgress. No Nightingale copy.
- Pets release readiness (no SQL/deploy/push/version bump): owner dog/weight/allergies/Medi Vet chat accepted. Phone Metro `:8081` → production `https://medicard.ge`. Hosted Neon already has 11 `Pet*` tables. Leftover `:4010` is not that session. OS reminders still unverified. See `docs/PETS_RELEASE_READINESS.md`.
