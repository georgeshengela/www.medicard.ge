# 2026-09-09

## Daily AI quota reset + refill ping (39.0.14)

Unused credits (e.g. leftover 1/3 sitting for days) now reset at the next Tbilisi midnight. Hitting 0/3 still starts a 24h lock from that exact moment — midnight does not unlock early.

Refill notification:
- Server sweeper every 30s sends Expo push (`quota-reset` / `quota-reset-lock`) and `usage:reset` socket.
- Quiet hours (22:00–08:00 Tbilisi) delay the ping to 08:15; the quota itself is already open.
- Client schedules a local backup at the same instant; duplicate banners are suppressed.
- In-app `QuotaReadySheet` (“ლიმიტი განახლდა” → ჰკითხე Medi-ს) when credits come back.

Stale leftover rows with no `resetAt` (old open-window model) zero immediately on the next `getUsage`. Additive `PeriodUsage.notifyAt`; runtime `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` so we don't `db push` (that currently wants to drop `UserLocation`).

## Cycle Phase 8 deploy + Phase 9 Journal UX (39.0.15)

Stopped Medicard `--watch` API processes that locked Prisma DLL, then `npx prisma generate` (client includes `CyclePredictionSnapshot`).

Neon had no `_prisma_migrations` and no snapshot table. Did **not** run `migrate deploy`. Applied additive SQL:

`npx prisma db execute --file prisma/migrations/20260908220000_cycle_prediction_snapshots/migration.sql`

Verified unique identity index, three query indexes, User cascade FK.

Phase 9 Journal: `პროგნოზების ისტორია`, first vs final expand, no accuracy score. QA seed: `server/scripts/cycle-phase9-qa-seed.js`.

## Cycle Phase 10 — Android prediction-history QA frozen

Stale Expo `fabGutter` crash was a leftover Metro bundle. Current source + `EXPO_PUBLIC_API_URL=http://10.0.2.2:4000` + `adb reverse`. QA login: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6!` (ADB `input text` does not update RN controlled `secureTextEntry` — reveal password first).

12 native shots in `qa/cycle-phase10-prediction-history/`. Empty Journal now still shows the history section. `many` seed state for >6 + `ყველას ნახვა`. Mobile **39.0.16**. iOS still deferred.

## Public calculator Georgian calendar

Native `input[type=date]` on `/calculators/*` is replaced by `calculators-calendar.js`: Monday-first grid, Georgian weekdays/months, day + month views, `დღეს`. Cache `calculators.css?v=2` / `calculators.js?v=2`.

## Cycle Overview gauge — no day dots (39.0.20)

Reverted the per-day bead necklace. Ring is the original Figma 4-pill progress fill + white knob. Fertile window is the same rounded stroke fill in purple (`c.fertile`), clipped to the four pills. Tap purple → date-range sheet. No diamonds, dashes, or day dots.

## Calendar day sheet duplicate estimate (39.0.21)

Tapping an ovulation day listed `სავარაუდო ოვულაცია` twice: once from the calendar mark, once from `displayPhaseLabel`. Same for predicted period. Sheet now skips the generic phase line when the day already has ovulation / fertile / period.

## Cycle Today row ignored most logs (39.0.22)

დღეს stayed empty after აღრიცხვა when the save was OPK / BBT / sleep / sex / tags — DaySummary only listed flow, pain, mood, symptoms, notes. Shared `cycleLogFactBits` now covers every observation. Quick Log applies the persist view immediately instead of waiting on a refetch that could miss the new row.

## Cycle save showed დაკავშირების შეცდომა

Not a log-form bug. Local API on :4000 was dying (`npm run dev` watch + EADDRINUSE / connection reset). Killed watchers, started **one** `npm --prefix server start` (no watch). Login + `PUT /api/cycle/logs/:day` OK. App force-stopped and relaunched; session still there. Do not start a second `npm run dev` while this process is on 4000.

Cycle QA lives in **Expo Go** (`exp://10.0.2.2:8081`). The installed `ge.medicard.app` APK is a stale JS snapshot (old teal horseshoe). Opening that APK looks like the redesign was deleted — it was not. After Metro `--clear`, reopen Expo Go, not the store/dev APK.

## Cycle Phase 12 — observation deploy + Android rich-tracking QA (39.0.26)

Stopped API, `npx prisma generate`, then additive SQL (Neon still has no `_prisma_migrations`):

`npx prisma db execute --file prisma/migrations/20260909140000_cycle_observation_registry/migration.sql`

`CycleLog.observations` JSONB `{}` + `observationSchemaVersion` INT 1. Old 26 rows readable.

QA seed `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a` (ADB cannot type `!`). Screenshots: `qa/cycle-phase12-rich-tracking/01–12`. Blank Expo Go was empty ExperienceActivity + stale Metro / missing `adb reverse`. Phase 11 tracking foundation final-frozen. Next product phase: Journal observation trends. iOS still deferred.

## Cycle Phase 13 — Journal observation trends FINAL-FROZEN (39.0.27)

Server-authoritative `GET /api/cycle/observation-trends` (180-day fetch, 30-day recent counts, up to 6 completed logged bleeds). Positive-presence only. No percentages, no causal copy, no Overview attach, no partner/AI/push/doctor-summary leak.

Journal keeps one `ტრენდები` section: observation rows first, then gated cycle-length bars (≥3) and PMS only when `hasPmsPattern`. Empty PMS shells and coverage % removed.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Shots: `qa/cycle-phase13-observation-trends/01–12`. Tests: `server/src/lib/cycleObservationTrends.test.js` (wired into `server/package.json` test script). Contract: `docs/CYCLE_OBSERVATION_TRENDS_CONTRACT.md`.

Next product phase at freeze time: Cycle doctor-summary observation inclusion. Completed the same day as Phase 14.

## Cycle Phase 14 — doctor-summary observation contract FINAL-FROZEN (39.0.28)

Dedicated clinician serializer `buildCycleDoctorSummaryData` + owner-only `GET /api/cycle/doctor-summary` (`private, no-store`). Registry `doctorSummary` default EXCLUDE. Fertility / sexual / notes are per-document opt-in, default off. PDF is on-device from structured facts (`cycleReport.ts`). Predictions stay off the PDF; estimates card is labeled separately. Moods excluded entirely. Partner / AI / personal-export serializers unchanged. Engine unchanged.

QA: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Artifacts `qa/cycle-phase14-doctor-summary/01–12` plus `00-payload-*.json` / `00-report-*.html`. Contract: `docs/CYCLE_DOCTOR_SUMMARY_CONTRACT.md`.

Next product phase: Cycle doctor-summary clinician locale rendering (en/fr/ru via existing i18n only). iOS Cycle QA still deferred. Not TTC / Pregnancy / Perimenopause / OpenRouter Cycle AI.

## Cycle Phase 15 — clinician locale rendering FINAL-FROZEN (39.0.29)

Same Phase 14 payload. Renderer localizes KA/EN/FR/RU. Catalog `mobile/src/i18n/cycle/doctorSummary.js`. One HTML builder `cycleDoctorSummaryI18n.js`. Document-scoped KA/EN/FR/RU chips on `/cycle/summary` (default ka, not persisted, does not enable sensitive toggles or change app language). No `en.ts`/`fr.ts`/`ru.ts` app catalogs — reused Quest `{ka,en,fr,ru}` pattern. Server has no `lang` param. Unknown enums omitted. Civil dates via Georgian month table or `Intl` + UTC noon. Tests: `cycleDoctorSummaryI18n.test.js`. Artifacts `qa/cycle-phase15-locale/` (01–12 + HTML). Next product phase: CYCLE TTC MODE FOUNDATION. iOS still deferred.

## Cycle Phase 16 — TTC MODE FOUNDATION FINAL-FROZEN (40.0.0)

`TRY_TO_CONCEIVE` is a live presentation mode. Forecast engine unchanged. Explicit settings onboarding. Compact Quick Log fertility block. Journal TTC timeline + honest BBT history. Removed fake OPK-cycle counts and BBT ovulation line from generic trends. `GET /api/cycle/ttc` 180-day owner read model. Contract: `docs/CYCLE_TTC_CONTRACT.md`. Seed: `server/scripts/cycle-phase16-qa-seed.js`. Zero new Notification Brain candidates. Android shots `qa/cycle-phase16-ttc/01–12`. Tests: `server/src/lib/cycleTtc.test.js` (14). Pregnancy / Perimenopause / OpenRouter Cycle AI still not product modes. iOS still deferred.

Next product phase: Cycle Pregnancy mode foundation (explicit user entry, no silent switch from a logged test).

## Cycle Phase 17 — TTC COLD-START RELIABILITY (40.0.1)

First `GET /api/cycle/ttc` on cold start could 401 before `GET /api/auth/me` / `GET /api/cycle`, and the catch stored that as empty TTC (`setTtc(null)`). Root mix: TTC ran before auth hydration (A), header could omit Bearer while `memoryToken` was still null (B), `/cycle` could succeed later while the first `/ttc` did not wait (D), and failures were cached as empty (F). No refresh-token flow (N/A).

Fix: shared `AuthContext.ready` + authenticated owner + server `mode === TRY_TO_CONCEIVE` + reachable; generation so a stale 401 cannot overwrite a 200; loading ≠ empty ≠ error. Hero copy: predicted period today now says menstruation is not logged, not “you have no records”. Five native cold starts: first TTC always 200. Android shots `qa/cycle-phase17-ttc-coldstart/`. Tests: `mobile/src/lib/cycleTtcQuery.test.js` + GET `/ttc` route registration. iOS still deferred.

## Cycle Phase 18 — PREGNANCY MODE FOUNDATION FINAL-FROZEN (41.0.0)

Explicit `PREGNANCY` mode. `CyclePregnancyEpisode` is source of truth (one ACTIVE). Confirm + reference date required. Positive test does not enter; negative does not exit. Server-owned week/day and estimated due date (LMP + 280). Overview/header show week/day, not luteal/cycle-day. Calendar and day details hide predicted period/fertile overlays. `GET /api/cycle/pregnancy` gated like TTC. Old `/cycle/pregnancy` redirects to `/cycle`. Forecast engine unchanged. Notification Brain unchanged. Perimenopause / OpenRouter Cycle AI not started.

QA: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Artifacts `qa/cycle-phase18-pregnancy/01–12`. Contract: `docs/CYCLE_PREGNANCY_CONTRACT.md`. Seed: `server/scripts/cycle-phase18-qa-seed.js`.

Next product phase: Cycle doctor-summary factual pregnancy header (mode + server week/day + estimated due date, no diagnosis). iOS still deferred.

## Cycle Phase 19 — PREGNANCY BABY SIZE FINAL-FROZEN (42.0.0)

Source-backed week catalog 1–40 (Hadlock 1992 CRL 5–18, WHO 2017 EFW 50th from 14, Williams CHL 20–40). `weekDevelopment` is attached from the existing Phase 18 completed week. No client gestational math, no engine change, no fruit on partner/AI/doctor/push. Overview hero + `/cycle/week/[week]` + Journal teaser. Android shots `qa/cycle-phase19-baby-size/01–12`. iOS still deferred.

Next product phase: Cycle doctor-summary factual pregnancy header (mode + server week/day + estimated due date; no fruit on the PDF).


Global `/api` 120/min/IP was one shared bucket. Chatty home + register + two `/me` reads showed `ძალიან ბევრი მოთხოვნა` on sign-up. Auth is off that bucket. API traffic is keyed per session (JWT prefix), not one IP for everyone. express-rate-limit validation disabled so a bad `X-Forwarded-For` cannot 500 the API. Concurrent first-register no longer races `FREE` package create. Register still returns a JWT if `getUsage` blips. Client keeps the new session if `/me` is 429/timeout — only a real 401 logs them out.
