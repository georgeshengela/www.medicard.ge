# 2026-09-18

## Test account

- Production user created: `n@medicard.ge`, fullName `women`, gender `FEMALE`, status ACTIVE, free package.

## Admin dashboard polish

- Socket.IO live pill now shows disconnect (`კავშირი დაიკარგა`) and online count (`ცოცხალი · N`).
- Overview hero KPIs share height (online card no longer stretches the four metrics).
- Hash `grain` stays on overview only; range still shared.
- Sidebar operations group renamed to **სერვისები** so it no longer duplicates overview **ოპერაციები**.
- Medi observatory: force grid over `.v3-module { display:flex }` so KPIs/charts stack under the toolbar.
- Users chips tightened. Missing-route panel is `aria-hidden` when not shown.

## Admin request speed (keep Socket.IO)

- Heartbeats no longer `clearAdminAnalyticsCache()` — charts stay 30s cached; `ops:live` still patches online/DAU/new-today.
- Overview is one `GET /analytics/dashboard` (+ `/balances`) instead of 8 Neon trips. Quality/health use bundles too.
- AppActivity range reads share a 4s in-flight memo; live snapshot still uses `fresh: true`.
- 8s HTTP poll skipped while the admin socket is connected; users table reloads only when `newUsersToday` changes.

## Welcome landing (დავიწყოთ)

- First screen uses Figma `8846:211832` mint concentric rings (`#5EEAD4` / 48px stroke) on brand 500→400, with a slow draw-in + drift/rotate and a traveling highlight on the middle ring.
- White Medicard vector mark + `მედიქარდი` wordmark; inverse white **დავიწყოთ** CTA. Version `1.0.0.8.66`.
- Welcome first-paint hitch: SVG dash drawing was the lag. Rings keep GPU rotate / drift / scale plus traveling highlights; appear is opacity+scale, not strokeDashoffset. Splash percent line box 76. Version `1.0.0.8.67`.

## Notifications (2026-09-18)

- iOS 26 change: brain/sync no longer call `requestNotificationPermission()` (silent deny). That also skipped Android channels and skipped the Expo Go gate, so reminders looked dead.
- Fix in `1.0.0.8.68`: load real `expo-notifications` when require() works (local reminders in Expo Go); recreate Android channels when permission is already granted; show the notification enable gate in Expo Go (still skip Health Connect there). Remote admin push still needs a store/dev build.

## Login logomark

- Sign-in used a soft/low-res looking mark (fat 206px shadow canvas + stroke halo; profile setup still used `logo-light.png` at 109px).
- Login now draws the Figma Authentication `Logomark` (11396:83995) as SVG: tight 36×36 viewBox, teal gradient `#0D9488 → #14B8A6 → #5EEAD4`. Version `1.0.0.8.69`.

## Login keyboard chrome

- შესვლა sat above the IME but kept home-indicator padding, so the gap under the button did not match the 16px gutter under the status bar.
- AuthShell now pads CTA with IME overlap + 16 (same as top gutter), compacting the hero (48px mark, subtitle out) in sync with the keyboard. Version `1.0.0.8.70`.
- Keyboard-open wordmark was clipped by scale + overflow. Full **მედიქარდი** now draws at 28px with extra line-height; tap outside a field dismisses the IME. Version `1.0.0.8.71`.
- Removed the hairline above შესვლა. Version `1.0.0.8.72`.

## Permissions toggle after setup grant (1.0.0.8.73)

- Setup Allow was ignored: we only treated Expo `granted` / `status === 'granted'`, so iOS authorized/provisional looked off. ნებართვები then re-called `requestPermissionsAsync` after a GET and showed `შეხსენებებისთვის საჭიროა შეტყობინებების ნებართვა`.
- Now honor `ios.status` 2/3/4, skip a second OS prompt when already allowed, and do not write opt-out on a failed re-read or Expo token miss.

## Permissions toggle still off (1.0.0.8.74)

- 8.73 still GET-probed OS status on ნებართვები focus, then re-asked on toggle. iOS 26 returns denied with no sheet → same toast.
- 8.74: page/gate/profile no longer `getPermissionsAsync` before an enable tap. Toggle follows in-app opt-in / remembered Allow. Failed OS ask opens Settings instead of only the toast. Fully close and reopen the app once.

## Permissions Settings dead-end (1.0.0.8.75)

- Owner: toggle-on opened სისტემური პარამეტრები even though iOS Settings already showed Notifications allowed. Expo `requestPermissionsAsync` was returning denied on the second ask.
- ნებართვები toggle on now only sets in-app opt-in (`enablePushConnection`). It does not re-ask the OS and does not send the user to Settings.

## Expo push token gone (1.0.0.8.76)

- Toggle on did not mint a token: Expo Go `getExpoPushTokenAsync` was stubbed to always throw, and login `syncPushRegistration` re-probed GET (false) so it never registered. Admin push page stayed empty.
- iOS Expo Go uses the real token API again. Opted-in sync skips the lying GET. Reload with toggle on should show the device on admin push.

## Expo Go toast (1.0.0.8.77)

- Owner saw Expo Go-ში შეტყობინებები აღარ მიიღება after toggle on. That copy is only true for **Android** Expo Go remote/admin push (SDK 53+). iOS Expo Go still supports Expo push; local reminders work on both.
- Toggle on now always says შეტყობინებები ჩართულია. `expo_go` register reason is Android-only.

## Admin push token still missing (1.0.0.8.78)

- Success toast was shown even when `getExpoPushTokenAsync` failed or the token was unregistered immediately. Register now always sends `projectId`, attaches a token listener (SDK 53 hang), and does not unregister on the opt-in path. Failed mint shows the token toast instead of a fake success.

## Expo token toast (1.0.0.8.79)

- Owner: `Expo token ვერ მივიღეთ` after 8.78. Extra `getDevicePushTokenAsync` + 20s timeout raced Expo’s in-module `nativeTokenPromise`; a timeout leaves that promise hung, so later toggles fail too.
- 8.79: mint only with `getExpoPushTokenAsync({ projectId, development: true })` on iOS Expo Go, keep `addPushTokenListener` for the whole JS session, 45s cap, toast includes the short error. Fully close Expo Go once so the hung promise dies.

## Admin test push not arriving (1.0.0.8.80)

- Token registered; admin send did not show on the iPhone. 8.79 forced `development: true` (sandbox APNs) on App Store Expo Go, which uses production APNs — Expo accepted the ticket, Apple dropped the notification.
- 8.80: auto-detect APNs environment, mark admin campaigns `type=admin_broadcast` (always show, even foreground / stringified iOS data), fetch Expo receipts so the admin toast shows DeviceNotRegistered vs fake OK. Remint: toggle off/on, lock the phone, send again.

## Owner verified (2026-09-18 evening)

- Setup grant, ნებართვები toggle, Expo token on admin მოწყობილობები, and a real admin test push on iPhone Expo Go all work. Do not force sandbox APNs. Do not GET-then-request on iOS 26. Do not race `getDevicePushTokenAsync` with a short timeout.

## Push FAILED banner (2026-09-18 night)

- Attention `4 კამპანია FAILED` was the 14:35–15:48 sends with **target 0** (no Expo token yet). After `george@medicard.ge` iOS token at 16:02, three campaigns SENT; 16:13 has Expo receipt `ok` (the one that arrived). Token still active. Empty-audience FAILED no longer pages Command Center.
