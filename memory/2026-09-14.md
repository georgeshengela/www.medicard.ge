# 2026-09-14

- Moved ლაბორატორია from Home to სამედიცინო ჩანაწერები (`records`). Public version `1.0.0.8.23`. No commit/push/deploy.
- Moved Medi Quest + Companion from Home to Profile (after the login-streak card). Home no longer mounts `mediQuest`. Public version `1.0.0.8.22`. No commit/push/deploy.
- Admin dashboard felt slow: HTML/CSS/JS are ~1–3ms; `#/overview` waited 3–4.5s on Neon (`/analytics/overview` ~30 queries) and every tab switch rebuilt the page. Health poll (8s) matched an 8s cache so almost always missed. Tightened: 15s admin-auth cache, 30s analytics cache, skip rebuild of already-painted tabs. First overview load still ~Neon. No commit/push/deploy.
- Stopped Medi Hunt and removed it from the working tree. Classic Medi Run stays. Hosted Neon Hunt tables are not dropped until `20260914180000_drop_medi_hunt` is applied. No commit/push/deploy.
- Home: removed the სირბილი (`run`) section. Run stays in the bottom tab only. Public version `1.0.0.8.20`. No commit/push/deploy.
- Removed Medi World from the working tree. Medi Quest and Medi Companion stay. Production Neon not mutated. Public version `1.0.0.8.3`. Not committed/pushed/deployed.
- Expo was offline because Metro was still compiling with `EXPO_PUBLIC_API_URL=http://10.0.2.2:4010` (disposable World Postgres). Stopped isolated `:4010`. Metro restarted on `https://medicard.ge`. Local `:4000` admin already uses production Neon. Same accounts: Expo + medicard.ge + localhost admin (login on each surface).
- Cleared junk from the repo: `qa-artifacts/`, `tmp/`, screenshot dumps, World inspect scripts, unused `maria.glb`, Viro AR plugin, World-only QR packages. Freeze shots in `qa/` stay (admin board + tests). Public version `1.0.0.8.4`. Not committed/pushed/deployed.
- Admin location flicker Tbilisi ↔ Liège: OS last-known GPS (often Tbilisi) was posted, then live Liège overwrote it. Stale `fixAt` is ignored; far moves clear the old city; a late Tbilisi geocode cannot overwrite Liège coords. Public version `1.0.0.8.5`.
- Pushed `9d87007` to `origin/main` (Render auto-deploy). Medi World removed; Medi Hunt shipped. Render `release` is generate+seed, **not** `db push`, so Hunt tables are not created by this deploy. `/api/hunt` 503 until additive tables exist. No EAS.
- Hunt map polish: viruses walk Mapbox walking routes (no 8×8 lane grid). 1 km square is play-area bounds only. 3D Standard map, day/night, speed HUD, iconed footer, ~10 m capsule/catch. Public version `1.0.0.8.7`. Not committed/pushed.
- Expo welcome screen: leftover empty `mobile/src/app` made Expo Router ignore real `app/`. Deleted it; pinned expo-router `root` to `./app`.
- Hunt 503 on production: applied additive Hunt SQL on Neon (`HuntConfig`, `HuntSession`, …). `GET /api/app/status` now `hunt.schemaReady: true`. Render code was already deployed; tables were the missing piece. No `db push`.
- Hunt Pac-Man arena: 1 km hole-mask, real Mapbox streets painted as lanes, ghosts/capsules/player are map-pitched GeoJSON (no HTML pins that drift on zoom). Public `1.0.0.8.8`.
- Admin Hunt 500: `/api/admin/hunt/overview` used `_req` then `req.query` (ReferenceError → generic Georgian 500). Restored `req`. Neon overview itself is healthy (schemaReady, 1 session).
- Hunt continuation: maze lanes = gameplay graph only (no viewport OSM snap). Coin defaults 2 / 5 / 15 / 30 and 300 m qualify. Weekly 8 captures (no extra coins). virus_1 collection as a variant. Interceptor uses a reachable junction. Public `1.0.0.8.9`. No deploy, no production migration this pass.
- Hunt defect-fix pass: encounter virus visible without inlined GLB; preview catch fixture; arena clip then reachability; endpoint-only preview joins; advisory lock + live cap min; kill switch on capsule/encounter. Pixel_8 Expo Go preview: camera+3D catch scene, 0 coins. Public `1.0.0.8.10`. No deploy, no production migration.
- Hunt actual-model pass: `virus_1.glb` loads via Metro fetch / chunked Base64 into the camera WebView (20,315 verts / 41,309 tris logged). Horror-ghost GIF as circular map-native pins (data-URL canvas blit). Preview still 0 coins. Public `1.0.0.8.11`. No deploy, no production mutation.
- Hunt visual polish: planted `virus_1.glb` framing (no spin/disc), horror-ghost atlas playback on compact map pins (~28 px), preview still 0 coins. Public `1.0.0.8.12`. No deploy, no production mutation.
- Hunt movement/fairness/readability: WebView path interpolation, 120–250 m graph spawn + 25 s grace, default flat 3D, stronger GLB idle, two-part capsules. Public `1.0.0.8.13`. Hosted `/api/hunt` unchanged until deploy. Preview 0 coins. No commit/push.
- Hunt remount handshake **1.0.0.8.15**: `movers: 0`/`seq: 0` after Fast Refresh was missed `ready` + one-shot snapshot (Android dropped later `injectJavaScript`). Generation hello/ack + 500 ms heartbeat. Overlay clears (`ingest` movers 4, seq 10 then 85…280). Footer in-flow below WebView. Measured preview clock. No `symbol-z-elevate`. ADB recovered via Pixel_8 `-no-snapshot-load` (no wipe). **Regression OPEN**: `along` stayed ~3–6 m on interchange GPS; 90 s video does not show a clear street walk. Coins 0. No deploy/commit/push.
- Hunt ghost travel **1.0.0.8.16**: root cause was patrol replanning every 2.5 s from the nearest node, snapping `alongM` to ~3–6 m. Patrol now keeps the current path; replans stitch from the current edge. 90 s L-fixture sim 88–118 m through a turn; live Mapbox preview `[hunt-sim]` e-3 87 m / 87 along / rev 0 at 89 s. Thin graphs return insufficient-network instead of stuck ghosts. **Still OPEN**: WebView timed out (`movers: 0`) so no on-map video of a turn; Pause tap not confirmed. Coins 0. No deploy/commit/push.
- Hunt bridge/Retry/Pause **1.0.0.8.18**: first missing event was ingest ack (hello timeout still fired in `layers`; Retry overlay eaten by Android WebView; Pause overwritten by `flushPing`). Compact snapshot, in-flow Retry remount, `commitPreviewTick`, QA urban-block on the normal preview CTA. Tests pass. **Still OPEN**: no Hunt-screen video of ingested movers / Pause / Retry. Coins 0. No deploy/commit/push.
- Hunt 503 on production: applied additive Hunt SQL on Neon (`HuntConfig`, `HuntSession`, …). `GET /api/app/status` now `hunt.schemaReady: true`. Render code was already deployed; tables were the missing piece. No `db push`.
- Home city lock **1.0.0.8.17**: Liège users were flipping to Tbilisi because grant stored Liège, then GPS watch + 15s heartbeat posted Android last-known Tbilisi and Nominatim rewrote `UserLocation` + profile city. Home place is grant-only now; heartbeat/watch no-op on client and ignored on server. Existing Tbilisi-on-Liège rows stay until re-grant. Hosted `medicard.ge` still old until a deploy. No commit/push/deploy.
- Medi chat quality: admin logs showed garbled Georgian + “is this you or a child?” on “ბავშვს 38.5”. Causes: profile age injected as the patient; Gemini 3.8 mandatory thinking + doctor `max_tokens: 900` truncating replies; Gemini errors falling back to Ling (invents fake Mkhedruli). Fix (prompt v1.6.0, server-only): caregiver rules, low reasoning, 2400 tokens, no Ling fallback. Needs Render deploy. No commit/push.
- Medi voice **1.7.0** / app **1.0.0.8.24**: friend + clear logic + few-shot Georgian examples; reasoning medium; first name once. Live-tested fever, slang, headache, BP. Empty chat: „გამარჯობა, მე ვარ Medi“. Needs deploy. No commit/push.
