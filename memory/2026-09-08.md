# 2026-09-08

## Admin chrome lane align (v3.69)

- Sidebar brand + page header share `--v3-chrome-*` metrics; bottom borders on one Y lane (18+6+40+18).
- Workspace `padding-top: 0`; header padding includes chrome pad-top so lanes match.
- Cache `?v=v3.69`.

## Push Brain redesign (v3.70)

- Full Brain observatory restyle: toolbar + range, KPI strip with icons, symmetric 2-col splits, decisions table.
- Charts rebuilt: labeled line charts (legend + Y ticks + area), sorted bar rows with %, funnel step conversion, heatmap legend, fatigue as horizontal % bars.
- Cache `?v=v3.70`, `ops-center.js?v=v26.11`.

## Admin tab width + funnel fix (v3.71)

- Standard: tab selector and tab content share the same left/right edges (Push + user investigation). Documented in `AGENTS.md`.
- Fixed compose panel inset vs subnav; killed legacy `.user-body` 22px padding on V3 user board.
- წარმატების ძაბრი: renamed to `v3-brain-funnel` so it no longer inherits modules.css multi-column `.v3-funnel`.
- Cache `?v=v3.71`, `ops-center.js?v=v26.12`.

## Health page redesign (v3.72)

- `#/health` native V3 observatory: toolbar + range, 5 KPI cards, feature chips, ranked comparison bars, focus panel + retention table.
- Matches Brain/Users spacing language; caps % at 100. Cache `?v=v3.72`, `ops-center.js?v=v26.13`.

## Medi/AI page redesign (v3.73)

- `#/ai` native V3 observatory: range toolbar, range-aware KPIs + chart (requests/errors/users), ops snapshot, quality scan + modules split, interactions table, scan history.
- Cache `?v=v3.73`, `admin.js?v=v27.10`.

## Rewards overview redesign (v3.74)

- `#/rewards?tab=overview` V3 observatory: toolbar, 6 icon KPIs, trend chart, attention panel with deep-links, recent ops table.
- Shared `v3-rewards-shell` locks subnav + pane to same width (all rewards tabs). Cache `?v=v3.74`.

## Packages page redesign (v3.75)

- `#/packages` native V3 observatory: toolbar, 5 KPI cards, distribution bars, billing notes, catalog rows, feature comparison matrix.
- Full override in `v3/modules/packages.js` (no legacy `.v25-pkg` wrap). Cache `?v=v3.75`, `admin.js?v=v27.11`.

## Orders page redesign (v3.76)

- `#/orders` native V3 observatory: toolbar + refresh, 4 clickable KPI cards, filter chips + search, queue panels (today / upcoming / meds / signups) with modern cards.
- Full override in `v3/modules/orders.js`. Range/grain still unused by API. Cache `?v=v3.76`.

## SMS page redesign (v3.77)

- `#/sms` native V3 observatory: toolbar, 6 KPI cards, send panel, log panel with status/purpose/search filters + pagination.
- Fixed user-only send to always include destination (API Zod). Full override in `v3/modules/sms.js`. Cache `?v=v3.77`.

## Pharmacy page redesign (v3.78)

- `#/pharmacy` native V3 observatory: toolbar, KPIs, source cards, sync form + live top-deal preview, top savings, failure alert, history with status filter + pager.
- Full override in `v3/modules/pharmacy.js`; auto-poll while RUNNING. Cache `?v=v3.78`.

## Quality page redesign (v3.79)

- `#/quality` native V3 observatory: toolbar + range, 4 coverage KPIs, policy grid, diagnostics issue cards, versions chart + permissions, weekly/insight/med minis.
- Surfaces hidden integrity fields; Georgianized EN labels. Cache `?v=v3.79`.

## Audit page redesign (v3.80)

- `#/audit` native V3 observatory: toolbar + CSV, 4 KPIs, action filter + debounced search, journal table with pagination, V3 detail drawer + raw JSON.
- Honest copy (range unused). Full override in `v3/modules/audit.js`. Cache `?v=v3.80`.

## Audit modal polish (v3.81)

- Detail modal was flush (`.drawer.is-modal .drawer-panel { padding: 0 }` without `.umodal` chrome). Rebuilt with umodal hero/body + scrollable body + spaced meta/diff/JSON blocks. Cache `?v=v3.81`.

## Settings page redesign (v3.82)

- `#/settings` native V3 observatory: toolbar + refresh, 4 status KPIs, panelled forms (availability / update / registration / QA / support), danger alerts.
- Preserved confirm toggles, dirty sticky save, PATCH flow. Cache `?v=v3.82`.

## Cycle circle gauge + rose chrome (39.0.7)

- Closed Figma `9001:283189` horseshoe into a true 360° gauge: 5 dotted segments, white/rose knob, faint Medicard mark inside the disc.
- Light Cycle hub uses Nightingale blush (`#FFF7F8` wash, `#F43F5E` CTAs, `#FECDD3` hairlines). Dark stays navy with rose accents.
- Today ring stays teal so today ≠ period. Fertile/ovulation stay violet estimates.
- Engine / privacy / notifications untouched.

## Cycle gauge 4-pill match (39.0.8)

- Figma track is 32px on Ø256, gap 16.2° between the three horseshoe pills.
- Closed circle uses 4 equal pills (N/E/S/W) with that same gap. Dots `#FDA4AF` `1 32`, track `#FFE4E6`.
- Rendered at kit scale (316). Logo stays faint inside the 186 disc.
