# 2026-09-06

- Location permission (**25.0.0**). After notifications, profile-setup asks for GPS. Existing completed users get a fade modal once. Grant reverse-geocodes via Nominatim (`Accept-Language: ka`), saves Georgian city + country + flag on `HealthProfile.extraAnswers.location` and `UserLocation`. Profile hero shows `🇬🇪 თბილისი, საქართველო`. Live coords ping while the app is active (watch + heartbeat). Toggle on `/profile/permissions`. Restart API for `POST /api/location`.

- Shipped the per-account weight management hub (app **22.0.0**). Home წონის კონტროლი tap now opens `/health-metrics/weight` instead of only the add sheet. Pencil on the hub still logs today's weight.
- Figma Nightingale screens implemented: details hub `8927:182138` (64px middle circle), history `8927:182116` (iPhone swipe, red circular trash), target `8927:185344` (kg/lb ruler), pace `8927:185730`, reminder `8927:187008`, confirm `8927:187231`, progress `8927:182883` (linear fire→flag), completed `8927:190025` + confetti.
- Storage is account-scoped (`medicard.weight.logs.v1`, `medicard.weight.goal.v1`, draft + daily Medi cache). Reminders use `NOTIF_PREFIX.weight`.
- Medi tips: local fallback immediately; `POST /api/ai/weight-advice` (OpenRouter `openai/gpt-4o`) on Consult Medi, 1 AI credit, no invented diagnoses. Server route must be deployed before production Expo hits it.
- Target-weight ruler must match registration (`AssessmentStepContent` weight step): integer kg 35–200 / lbs 80–400, `labelEvery={5}`, `labelOrigin` 35/80, default 10pt ticks. Do not use 0.5-step values or custom `tickWidth`. App **22.0.1**.
- Pace screen (**22.0.2**): slider is RNGH pan (not stack swipe), last stop fills 100%. Medi recommends pace+deadline from profile (`recommendWeightPace`) — no credit burn, no diagnoses. Deadline shows full date + weeks. `paceSource` keeps a user override.
- Goal nav (**22.0.3**): confirm → started progress (wizard dismissed). Started CTA/back go to weight hub, never back into target. Home weight card always shows compact goal bar when a goal exists. Copy: `კიდევ N კილოგრამი უნდა დაიკლოთ! რომ მიზანს მიაღწიოთ!`
- Today-weight save (**22.0.4**): `შენახვა` was failing because `logManualHealthMetric` required `/health-metrics/sync` + a full `extraAnswers` PUT. Now it writes the local log first, caches the daily row, and treats server sync as best-effort with a slim `{ weightKg, firstHealthMetricLogged }` patch.
- Current weight must be one number (**22.0.5**): home was profile/metrics, hub was stale `weight.logs` / week seed. `resolveCurrentWeightKg` + `upsertTodayWeight` — logging today does not change `targetKg`.
- Notifications audit (**22.0.6**): med reminders now honor `daysOfWeek` (WEEKLY vs DAILY). Weight/steps get Android channels. Tap opens the matching screen. Home teal **bell** FAB (DEV) lists every push type, what it sends, and fires a real banner in 2s. Refill toggle is saved but still not scheduled — remaining count is not decremented on dose take.
- Push templates (**22.0.8**): admin `#/push` now edits Medi copy for every local reminder. QA/local fires log to `PushEvent` so they show in admin. Broadcasts stay on `PushCampaign`. Copy is warm Medi + emoji; `{name}` `{dosage}` `{kg}` interpolate. Needs server deploy + `db push` for `PushTemplate` / `PushEvent`.
- Medi Notification Brain (**23.0.0**): scored local companion pushes (check-in, streak, weekly report `/week`, insights only on real signals, achievements, hydration, quiet-day steps, sleep, re-engage 2/5/14/30, birthday, one profile question, chat follow-up, optional feature). Quiet hours 22:00–08:00, frequency rare/balanced/often, discreet mask. Settings at `/profile/notifications`. Admin `#/push` has an Engaging tab for copy. Brain reschedules on app open and does not nag if the user just opened Medi.
- Notification Brain polish (**23.0.1**): per-family cooldowns, fatigue score (ignored opens reduce cap without changing the user's setting), preferredEngagementWindow from a 30-day open histogram, unfinished-draft + post-visit + missed-dose insight candidates, lock-screen actions (take / drank / snooze / chat), strict deep links, DEV “why did Medi send this?” trace. Weekly admin label is Sunday „ჩემი კვირა Medi-სთან“ report. Cache `admin.js?v=push6`.
- Notification Brain v1 complete (**23.0.2**): `selectedFrequency` / `baseDailyCap` / `adaptiveDailyCap` stay distinct — fatigue never writes the user’s frequency. EWMA so one open does not jump the cap. `revalidateBeforeDelivery` via the OS handler + `requestEngageRefresh` when water/doses/drafts/visits/week/pain change. Decision IDs `notif_dec_…` with created/scheduled/revalidated. Deleted med/visit routes fall back to the list. QA scenarios covered in `mediNotificationBrain.test.ts`.
- Medi push copy + admin UX (**22.0.9**): final Georgian titles/bodies (meds, cycle, visit, activity). Interpolator strips empty vars so `{dosage}` / `{place}` / empty `{doctor}-თან` do not leave junk. Discreet mask has no period/ovulation/PMS/pregnancy/med words. Admin intro + variable legend; per card restore/save with gap; keys hidden unless Advanced. Weight reminder uses last logged kg; steps uses cached today count when present. If a `PushTemplate` row is already custom in Neon, reset it to pick up new defaults.
- Admin `#/push` redesigned as a studio: hero pulse + KPI row, tabbed workspace (გაგზავნა / Medi ტექსტები / ისტორია / მოწყობილობები), iPhone lock-screen preview, segment pills, in-panel confirm, live interpolated template preview, searchable history cards. Cache `?v=push4`. `getPushStats` now returns platform counts. Restart the API server to pick up the stats field.
- Admin Command Center (**ops1**, mobile **23.0.3**): `/admin` overview is Operations & Intelligence on real Tbilisi-day aggregates. New endpoints under `/api/admin/analytics/*` and `/api/admin/system/health`. Notification Brain decisions sync via `POST /api/push/decisions` into `NotificationDecision` (raw SQL ensure). Admin audit on template/settings/user status. No fake KPIs. Opens/actions/weekly report/dose-taken stay device-local until more sync exists. Restart API + hard-refresh admin `?v=ops1`. Neon: `db push` for schema indexes; new tables also `CREATE TABLE IF NOT EXISTS` on first write.
- Admin Command Center copy is Georgian (**ops3**). KPI tooltips, attention banners, feature labels, Brain notes, command palette, Engage tab, and login chrome were still English after ops1/ops2 — those now come from the API/UI in KA. Product names stay Medi / Brain / Push / iOS / Android. Hard-refresh `/admin?v=ops3` and restart the API so cached analytics strings refresh.
- Admin V2.1 measurement layer (**ops4**, mobile **24.0.0**). DAU/WAU/MAU switched from DailyCheckIn to AppActivity. Notification outcomes sync (`POST /api/push/outcomes`) without title/body/health. Lock-screen actions count without an app open. Weekly/insight/dose/permission events are sanitized. Hard-refresh `/admin?v=ops4`, restart API (60s analytics cache), Neon `db push` for new tables. Brain tests still 35/35.
- Admin V2.2 operator UX (**ops5**, mobile **24.0.1**). Command Center is 3-level: KPIs → activity/growth/features → Brain + attention + system. Users activity filters use AppActivity. Version policy is centralized. Weekly opened ≠ generated. Hard-refresh `/admin?v=ops5`.
- Admin V2.3 user investigation + decision debugging (**ops6**). Existing user drawer is now a 6-tab operator profile. Decision drawer shows lifecycle, inferred client context (`contextSource`), and telemetry capability from `appVersionPolicy`. No new Command Center pages. Hard-refresh `/admin?v=ops6` and restart API.
- Admin V2.4 visual refinement (**ops7**). Same V2.3 data/architecture. Labeled 212px sidebar, `admin-v24.css` tokens, KPI strip, chart language, drawers/tables, Brain funnel branches. Hard-refresh `/admin?v=ops7`. No mobile bump.
- Users registry table polish (**ops8**). Fixed 8-column colgroup, toolbar is search/count/actions then compact chips, quieter cells (identity / quota / platform / last-active). Hard-refresh `/admin?v=ops8`.
- User profile modal polish (**ops9**). Same 6 tabs and actions. Shared 20px gutter, 3×2 stats strip, pinned footer, quieter hero (no პროფილი kicker), form sits under ანგარიშის რედაქტირება. Hard-refresh `/admin?v=ops9`.
- User profile is a page now (**ops10**). Row click opens `#/users/:id` instead of a modal. Back goes to the registry. Same 6 tabs and edit/save/delete/reset. Hard-refresh `/admin?v=ops10`.
- Admin visual polish (**ops13**): icons back on KPIs, users strip, health chips, settings, Brain/overview heads; compact empties; no duplicate page titles; settings two-column on 1440. Hard-refresh `/admin?v=ops13`.
- Admin V2.5 page-by-page visual redesign (**ops12**). `admin-v25.css` gives each existing route its own IA (overview hero, users table-first, user-page tabs, Brain funnel/fatigue/heatmap, health chips+rank+return, quality diagnostics+version policy, audit inspector, Push compose+preview, packages list, settings groups, SMS/pharmacy ops strips, login card, 404). No new metrics, no Brain logic change, no fake data. Screenshots at 1366/1440/1920 in `server/scripts/_v25-shots/`. Hard-refresh `/admin?v=ops12`. No mobile bump.
- Users KPI strip polish (**ops15**). მომხმარებლები cards were leftover `.orders-kpi` boxes (stripe, radius, lift, 32px stack) inside the V2.5 strip. Now one 4-cell strip matching Overview: icon+label / number / hint, no card chrome, no default mint wash on სულ. Hard-refresh `/admin?v=ops15`. No mobile bump.
- Users table spacing (**ops16**). Registry is 6 columns in operator order (პირი → სტატუსი → პაკეტი → ბოლო აქტივობა → პლატფორმა → აქტივობა). Quota/registration stay on the user page, not ghost-hidden in the table. Search, filters, cells, and pager share a 16px gutter. Hard-refresh `/admin?v=ops16`. No mobile bump.
- Health page redesign (**ops17**). `#/health` is a feature observatory: 8-cell strip, ranked comparison (zeros included), always-on focus (leader or `?feature=`), return table. Feature links keep the range. 7დ/30დ still hide below N=5. Hard-refresh `/admin?v=ops17`. No mobile bump.
- Push studio redesign (**ops19**). `#/push` drops the pulse hero for a 6-cell KPI strip, underline tabs, and a range bar for Brain. Compose + lock-screen preview stay. No Brain logic or metric changes. Hard-refresh `/admin?v=ops19`. No mobile bump.
- Live presence (**ops18**, app **24.0.2**). Last-active was only `/me` + resume. Now every screen, a 15s heartbeat, and resume write `AppActivity.lastAt` and bump Expo `PushToken.lastSeenAt`. Admin overview/users/push refresh every 8s (skip while typing/scrolling). Analytics cache 8s. Restart API.
- Decisions tab redesign (**ops20**). #/push გადაწყვეტილებები is log-first: 7-cell funnel strip, search table, then funnel/suppress, fatigue/heatmap. Empty time charts stay hidden. Same APIs and click/CSV/drawer behavior. Hard-refresh /admin?v=ops20. No mobile bump.
- Orders queue redesign (**ops21**). #/orders შეკვეთები is a 4-cell KPI strip, underline filters, and a quieter board. Unused columns hide with the filter. Same /orders data and user-open clicks. Hard-refresh /admin?v=ops21. No mobile bump.
- System pages redesign (**ops22**). `#/packages` `#/sms` `#/pharmacy` `#/quality` `#/audit` `#/settings` now use the same flush KPI strip + panel language as Users/Decisions/Orders. No new metrics or fake data. Hard-refresh `/admin?v=ops22`. No mobile bump.
- Overview live (**ops23**). `#/overview` no longer rebuilds every 8s. Socket.IO pushes `ops:live` (DAU / online from AppActivity.lastAt) and patches KPI numbers in place. Restart API. Hard-refresh `/admin?v=ops23`. No mobile bump.
- Weather Wellness (**26.0.0**). Compact Home card + `/weather` detail. Open-Meteo from existing GPS coords, 20-min cache, deterministic Medi copy (ka/en/fr/ru keys), outdoor window scoring, Notification Brain family `weatherWellness` (max 1/day, 22h cooldown, revalidate before send). No second location prompt. No diagnoses. Lock-screen copy never includes steps/ml.
- Weather Home compact + footer fix (**26.0.2**). Home card dropped the hourly chips and big 52px temp stack; one row + Medi line + window pill. Weather page no longer dumps `category=` / `score=` / `reasons=` at the bottom (that was a DEV debug block showing in Expo Go).
- medicard.ge landing now lists **ამინდი და Medi** (hero, ticker, features, FREE plan). Push to `main` deploys web via Render.
- Weather air quality (**26.0.3**). Open-Meteo Air Quality API (no key) in parallel with forecast. European AQI + PM2.5 / PM10 / O₃ / NO₂. Compact Home chip; full block on `/weather`. Poor air (≥60) blocks walk pushes; extremely poor (>100) disqualifies outdoor hours. Weather still works if AQ fails.
- Weather Home redesign + load flash (**26.0.4**). Card is one tight row + icon chips (clock / air / rain). AQ uses Wind / Leaf / fog / alert icons. App open shows „ამინდი იტვირთება…“ and last cache — never the failed-to-load error while the fetch is still running.
- Medi Quest Phase 3 (**27.0.0**). First user-facing Quest: Home module + `/medi-quest` hub, claim/XP/coins/level-up, Socket.IO user rooms. Hydration idempotency via `HydrationIntakeEvent`. Steps quests only if capability is AVAILABLE. Local hydration goal wins pre-launch. Stop before achievements / store / social.
- Medi Quest Phase 3.5 (**27.0.1**). Visual refinement only: shared Quest tokens, quieter Home module, Hub header without HUD chrome, loyalty coin mark, grouped history, level-up as a calm overlay. No economy/API changes.
- Medi Quest Phase 3.6 (**27.0.2**). Live visual QA freeze on Pixel_8 Expo Go. Metro `socket.io-client` → `dist/socket.io.js`. Android Expo Go no longer imports `expo-notifications` (`src/lib/expoNotifications.ts` stub). Reward float is an overlay. Home claimable drops the daily bar. DEV Quest fixtures + visual session (`DEV: Quest UI` on welcome). History/wallet fixtures. Progress min-fill, capped over-target steps, no `sourceType` / `periodKey` leak. **Medi Quest Mobile UI v1 frozen.** No Phase 4. iOS not live-tested (Windows). Production Quest API not used this pass.
- Admin V2.6 full visual redesign (**v26.2**). New `server/admin/admin-v26.css` layer (loads last) restyles every primitive and page: labeled sidebar with groups, sticky blurred topbar, pill filters/badges, unified KPI strips (26px values), softer cards/shadows, refined tables/pagers/tabs, split login with feature rail, drawer/modal/toast polish, custom scrollbars. Same palette, all gradients removed (login rail, avatar, umodal hero, users toolbar, trend bar, skeleton shimmer), calm motion only, reduced-motion kills all animation. Fixed selector leaks: `.orders-col b` styling feed-row names, global input sizing inflating checkboxes, audit card icon-tile stretch, package badge stretch. Light+dark verified via `server/scripts/_v26-shots.mjs` (32 screenshots in `server/scripts/_v26-shots/`). Hard-refresh `/admin?v=v26.2`. No mobile bump.
- Metro EMFILE fix + Quest null profile (**27.0.3**). Metro on Windows crashed with `EMFILE: too many open files` on the first HMR rebuild: Expo's `@expo/metro-file-map` fork defaults `useWatchman` to off, so Windows used FallbackWatcher = one `fs.watch` handle per directory (~7k under node_modules). Installed Watchman via `winget install facebook.watchman` and set `config.resolver.useWatchman = true` in `mobile/metro.config.js` (falls back silently when the binary is missing). Also: server `emptyQuestDashboard` returns `profile: null, unavailable: true` when quest tables are not provisioned; mobile typed `profile` as non-null and Home crashed with `Cannot read property 'level' of null`. `QuestDashboard.profile` is now `QuestProfile | null`, `homeModuleView` treats no-profile/unavailable as `empty`, hub renders `QuestHubHeader` only with a profile (prop is `profile` now). Restart Cursor terminals to pick up the new PATH before `npm start`.
- Medi Quest level-up modal (**28.0.1**). Rebuilt from Figma `8853:146377`: centered card (32px radius), 320px dotted-halo score disc (`QuestLevelHalo` — exact ring r/stroke/dash from exported ellipses), mint inner disc, `#22C55E` check badge, title + body, teal CTA, navy X below the card. Copy is Medi (`ახალი დონე` / `გილოცავ — ახლა N დონე გაქვს.`), never Nightingale. Slow halo spin, reduced-motion static.
- Medi Quest UI v2 (**28.0.0**). Full audit + redesign of Home module, `/medi-quest` hub, history, wallet on Pixel_8 Expo Go light + dark. New primitives: `QuestLevelRing` (SVG ring, reanimated), `QuestSegmentedProgress` (one segment per daily mission, staggered fill), `QuestStatChip` (coins / streak), `QuestClaimButton` (press spring + slow sheen). `QuestProgressBar` / `QuestReward` / `QuestIcon` animated + tokenised (`QUEST.wash`, `QUEST.pill`, `QUEST.motion`). Home: Medi Quest moved directly under the header score card; ring + chips + segments + claim strip; reward float anchored top-right of the section title so it never covers the card title; compact `xpLeft` copy. Hub: hero header with 96px ring, rank, XP bar, chips; cards stagger in; helper text on its own row; claimed pill in the reward row; centered error state; `—` for zero streak. History: pills under the title. Pure helpers `dailySegments` / `levelRingProgress` / `dailyCounter` in `logic.js` (23 tests). Two Android rendering bugs fixed: NativeWind drops function-style `Pressable` styles without `className`; Reanimated warns when static `opacity` shares an `Animated.View` with `entering`/`layout`. No API / economy changes. Screens in `mobile/store/_qa-quest-v2/` (ignored).
- Brain tab realtime (**v26.3**). The 8s `refreshAdminLive` no longer calls full `renderPush()` (that was the visible auto-refresh on history/devices/decisions). New flow: `POST /api/push/decisions|/outcomes` -> `notifyBrainSync()` in `adminRealtime.js` (1.5s debounce, clears analytics cache) -> `brain:sync` to admin room -> admin.js (600ms debounce, skips when hidden/typing/drawer open) -> `patchPushBrainLive()` in ops-center.js patches strip numbers, rates, and dec log in place (keeps search/filter, rebinds row clicks). E2E verified with QA OTP user (+995500000005 / 0000): evaluated bumped live, DOM marker survived. Also: funnel numbers explained — `evaluated` = on-device Brain evaluation rows (every app open x every family), not sent pushes; only `delivered` counts real lock-screen deliveries. Restarted API (killed stale non-watch node 39708; --watch child now owns :4000).
- Weather Home card redesign (**28.0.2**, reworked in **29.0.1**). Home section title is now just „ამინდი“ (`ka.home.weatherTitle`; detail screen keeps „ამინდი და Medi“). First pass (chips + wrap) still read asymmetric, so v2 layout is: header row (34px temp + condition·city left, 56px `WeatherEmblem` right), hairline divider, `Sparkles` + Medi advice (2 lines), then **three equal-width StatTiles** (icon / bold value / label, centered, `adjustsFontSizeToFit`) picked by priority — window → air → rain → feels-like → wind — so it's always exactly 3 symmetric tiles, never ragged chips. Mood LinearGradient bg (white→wash light, surface→washDark dark), one soft circle, translucent tile fills. Air band label derived via `ui.air(band).replace(/^ჰაერი\s*/, '')`. Skeleton matched (~168px, 3 tile bones). the Neon DB (shared by localhost and medicard.ge) had none of the quest tables: Render never ran `prisma db push` and a local push aborts on the runtime-created `UserLocation` table (not in schema, 1 row -> data-loss warning). Applied only the additive part of `prisma migrate diff` (8 tables: QuestTemplate, UserQuest, UserQuestProfile, QuestCompletion, RewardLedger, HydrationIntakeEvent, HydrationPreference, StepTrackingCapability + indexes/FKs) via `prisma db execute`, then `npm run seed` (4 templates). Verified on prod with a throwaway account (deleted). Remaining schema drift NOT applied: DROP UserLocation, NotificationOutcome.actionKey NOT NULL, FKs on AppActivity/NotificationOutcome/ProductEvent/MedicationDoseEvent/NotificationPermission/NotificationDecision, 3 index renames. Local server needs `prisma generate` + restart (client was stale).

- Medi Quest Phase 4 — Achievements & long-term progression (**29.0.2**). Quest UI v2 stays frozen; Phase 4 is additive. Server: `AchievementDefinition` + `UserAchievement` (immutable unlocks, `@@unique([userId, achievementId])`), additive SQL via `prisma db execute` (db push still blocked by UserLocation drift), 50 achievements in `achievementDefs.js` (rarity tiers COMMON→LEGENDARY drive rewards: 25/50c…; XP capped by `maxSingleAchievementXp` 1000), evaluator in `achievements.js` (counters from QuestCompletion/RewardLedger/UserQuestProfile, unlock+reconcile after every quest complete/claim, secret masking in overview), explicit claim → RewardLedger `sourceType: 'ACHIEVEMENT'` + XP/level cascade with same level curve, routes `/api/achievements` (GET overview, POST /:id/claim, POST /reconcile), Socket.IO `achievement:unlocked` / `achievement:claimed`. 20 new server tests (381 total green). Mobile: `useAchievements` hook (cache + dev fixture), `/medi-quest/achievements` collection screen (hero counter, category groups, rarity chips, progress bars, claim buttons, secret rows masked), hub preview strip (`achievementsPreview`), rarity-tinted unlock toast in QuestHost, claim → reward float + global level-up sheet reuse on cascade. `QUEST.rarity` palette light/dark. ka/en/fr/ru pattern copy in `src/i18n/quest/achievements.js`. 31 mobile logic tests green. Visual QA on Pixel_8 Expo Go light+dark via DEV visual session: emulator had no DNS so `medicard.ge` health failed → offline gate disabled claim buttons; QA ran with `EXPO_PUBLIC_API_URL=http://127.0.0.1:4000` + `adb reverse` (env restored to prod after). DEV Quest states switcher got an "Achievement unlock toast" trigger. AuthContext visual-session typing fixed (tsc clean for Phase 4 files; ~71 pre-existing unrelated tsc errors remain in weather/privacy/weightNav). Stopped before Smart Quest / Rewards Store / social per spec.

- Phase 4.1 achievement economy integrity audit (**29.0.3**). The Phase 4 implementation WAS wrong: `achievementDefs.js` derived rewards from a `RARITY_REWARDS` table; the spec requires exact per-achievement values (e.g. FIRST_QUEST 20/10 not 50/25, STREAK_365 1500/500 not 750/500, WEEKLY_3 100/50 while QUESTS_5 is 30/15 — same rarity, different rewards). Fixed: catalog now carries explicit rewardXp/rewardCoins per definition; `RARITY_REWARDS` deleted; rarity is presentation-only. `maxSingleAchievementXp` 1000 → 2000 (STREAK_365 needs 1500; ceiling rejects, never clamps). Claim path was already correct (reads DB definition values, never rarity). No data impact: DB had 0 definitions / 0 unlocks / 0 achievement ledger rows (lazy seed had never run; all QA was fixtures) — seeded 50 corrected defs via `server/scripts/_p41-seed.mjs`. New tests: strict 50-item economy snapshot (rarity/threshold/rewards/isSecret), same-rarity-different-rewards proof, STREAK_365 ceiling pass, 5-key exact claim-path (FIRST_QUEST/QUESTS_500/STREAK_365/EARLY_BIRD/LEVEL_30). Server 393 green (was 381), mobile 31 green. Mobile DEV fixture sample rewards aligned to canonical values (display only — mobile never computes economy). No Phase 5.

- Medi Quest Phase 5 — Smart Quest Engine (**30.0.0**). Server: `smartQuestEngine.js` (SMART_QUEST_ENGINE_VERSION=1) — 14-day median step baseline (min 3 valid days, sanity ceiling 100k, nulls/ambiguous zeros excluded), deterministic target bands (B<2500 / <5000 / <8000 / >=8000 with per-band clamps, roundTo250, hard max 10000), 7-quest performance buckets (STRUGGLING <0.40 -> -10%, floor 2000; STABLE_HIGH never escalates), comeback (>=8-day gap + prior history -> min(personalized, roundTo250(max(2000, B*0.90)))), weekly = roundTo1000(B*7*0.90) clamp 21k–60k (default 35k, struggling -10%). Wired into `createAssignment` for STEPS templates only; target + `metadata.smart` (engineVersion/targetSource/difficulty/reasonKey/baselineBucket/performanceBucket/comebackMode) frozen per periodKey; engine failure falls back to template default. Rewards untouched (daily_steps stays 50/30 at any target). `reconcileQuestEligibility`: ACTIVE STEPS quest -> CANCELLED when capability lost, HYDRATION -> CANCELLED when goal removed, restored to ACTIVE if eligibility returns same period; COMPLETED/CLAIMABLE never touched; dashboard filters CANCELLED (segmented bar denominator reconciles). API adds targetSource/difficulty/reasonKey via `publicQuest`; `smart_quest_assigned` product event logs buckets only (never exact target). Mobile: `smartContext.js` priority chain pain > comeback > evening(<50% after 20:00) > near-completion(>=80% before 20:00) > weather(rain/UV/wind/severe/good-window from useWeather) > struggling > personal-baseline > morning > default; copy via `movementContextLine` (one line on QuestCard + Home priority quest), deterministic rotation `smartContextSeed` (questId+periodKey+contextKey), "რატომ ეს მიზანი?" bottom sheet (personalized/comeback/default explanations, §36 wording), ka/en/fr/ru in catalog `smart`/`smartWindow`/`whyTarget`. No pain boolean exists server-side (mobile `loggedPain` accepted but unwired — documented limitation). Weather stays mobile-only context; server assigns weather-blind. Tests: 49 engine + 14 mobile smartContext new; suite 456/456 green. Live Pixel_8 QA (light+dark) via DEV fixtures (COMEBACK/STRUGGLING/DEFAULT_TARGET scenarios added): evening rule observed live at 23:xx (gentle copy <50%, near-completion suppressed after 20:00), why-sheets verified for all 3 sources, dark ok. Welcome DEV launcher floated (top 220) because 2400px screen pushed it off-screen. `.env` restored to prod after QA. No Notification Brain / Rewards Store / admin UI.

- Medi Quest Phase 6 — Smart Quest × Notification Brain (**31.0.0**). Client-side Engage Brain remains the ONLY scheduler. New family `questSmart` (topic toggle, 20h cooldown, max 1/local-day via family-day send). Candidate types: QUEST_NEAR_COMPLETE / QUEST_GOOD_WEATHER_WINDOW / QUEST_COMEBACK / QUEST_MORNING_PLAN / QUEST_WEEKLY_PROGRESS — pure module `questSmartEngage.js` picks the single best scored candidate per evaluation (base scores 78/72/74/50/60 + modifiers). Revalidation mandatory with explicit QUEST_* reason codes. Deep link `/medi-quest?from=push`. Lock-screen copy ka (ENGAGE_FALLBACKS + pushEngageTemplates); no reward urgency / streak fear / medical claims. `requestEngageRefresh` on quest cache threshold crossings (50/75/80/90/100) and status/period changes. DEV weather fixture (GOOD_WINDOW/RAIN/HIGH_UV/WIND/SEVERE/UNAVAILABLE) never writes LIVE cache. Pain suppression reuses existing Brain `loggedPain` (cycle entries) — no new medical pipeline. Outcome `actioned` attribution for Quest progress NOT faked (limitation). Tests: 19 questSmartEngage + 6 Phase 6 brain cases; suite 475 + engage 38 green. Stopped before Rewards Store / social / admin.
