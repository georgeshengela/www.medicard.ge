# 2026-08-31

Profile tab upgraded: hero with package chip + phone/email, health facts (age, birth, height, weight, BMI, blood type, smoking, allergies, conditions), legal menu, support mailto, in-app account deletion.

Privacy Policy and Terms are full Georgian documents at `/profile/privacy` and `/profile/terms`. Same text is served publicly at `/privacy` and `/terms` for App Store Connect.

`DELETE /api/auth/me` permanently deletes the signed-in user (reuses admin `deleteUserAccount`). Client also clears push, reminders, session, and Cycle offline cache.

App Store prep: Face ID + microphone usage strings, hide stub Google login, legal URLs in `app.json` extra. Expo **18.0.10**. Needs server deploy before production delete/legal URLs work.

Landing rebuild (`server/public/`): editorial marketing site with real store screens (home, cycle, Medi, metrics, meds, streak, profile, hydration, sign-in, symptoms). Light/dark theme persist (`medicard.landing.theme`), scroll reveals, ticker, 15 feature cards, FREE/STANDARD/ULTIMATE compare table. No CSS gradients. Copy uses Medi (not Nightingale). Cycle described as female-only, server-owned forecast. Not committed/deployed until asked.
