# 2026-09-07

## Medi Run: "stuck on áááá áááĄááŁááá" â root cause + redesign (v32.0.1)

- **Root cause of the stuck bug:** NativeWind v4 (`react-native-css-interop`) does NOT support `Pressable` function-style callbacks (`style={({pressed}) => ({...})}`). The callback is never called â styles dropped entirely â the áĄá˘áá á˘á button rendered as invisible white text on the white glass card. Known upstream issue (nativewind #1105/#1128). Confirmed live: tapping the invisible button DID start the run.
- **Fixes shipped:**
  - `app/_layout.tsx`: added `run` group with `headerShown: false` (was showing an ugly duplicate "run" system header).
  - `app/run/active.tsx`: redesigned ready card â full-width teal START (h58, play badge), compact dice reroll icon button in header row; static styles.
  - `RunHud.tsx`: HudRoundButton + big pause circle â static styles (controls now render as real circles).
  - `RunTargetSheet.tsx`: CTA static style (was invisible), custom-value chip now flexGrow full-width (placeholder no longer truncated), more bottom padding.
  - `app/run/index.tsx` hero CTA + `HomeRunSection.tsx` card wrapper â static styles.
  - Version 32.0.0 â 32.0.1.
- **E2E verified on emulator via adb** (screencap + input tap): login QA user (+995500000005 / OTP 0000, deep link `exp://127.0.0.1:8081/--/phone`), home â hub â sheet â pin drop (Mapbox route drawn) â START â simulation movement (time/distance/pace/kcal/steps counting) â finish confirm â summary â home card shows last run. All screens look right now.
- **Pending debt:** ~30 other files (cycle, home sheets, profileâŚ) still use the Pressable function-style pattern and are likely silently unstyled since NativeWind was added. Needs an app-wide sweep. Noted in workspace AGENTS.md.
- **adb screenshot workflow gotcha:** PowerShell `>` corrupts binary; use `cmd /c "adb exec-out screencap -p > file.png"`, then System.Drawing downscale to ~420px JPG for viewing.

## Medi Run v33.0.0 â game chase-cam + vehicle detection

- **Chase cam (mapHtml.ts):** while following â zoom 18.3, pitch 67, bottom padding 44% so the runner puck sits lower-third and the lane stretches ahead; camera bearing = heading. Route drawn as a road-lane (glow + casing + solid teal + animated white flowing dashes via Mapbox dasharray ant-line, 75ms step). Route ahead trimmed per fix (pointer advances along vertices; lane = [current pos, ...rest]). Trail recolored amber. New SVG markers: teal glossy puck + white nav chevron (rotates with heading); hand-crafted glossy 3D amber pin (SVG gradients) with light beam, pulsing ground halo, bob animation; captured â hue-rotate to green + pop.
- **CRITICAL Mapbox gotcha:** programmatic `easeTo` with bearing change fires `rotatestart`/`dragstart` too â a naive "user interacted â stop following" handler instantly kills follow. Must check `e.originalEvent` (only set for real gestures).
- **Speed + transport detection (store.ts):** raw speed computed per fix BEFORE the GPS-jump filter (which rejects >43 km/h fixes from distance); EMA-smoothed `speedKmh` in state, shown next to the clock in the HUD. Sustained >25 km/h: warn at 12s (amber banner "áŤááááá áĄáŹá áá¤áá áááááŽáá  đ" + amber speed), auto-cancel at 32s (`cancelForTransport` â phase idle + `transportCancelled`, modal with car icon explains walking/running only, run NOT saved). <18 km/h decays the counter 2Ă.
- **Dev sim now 3 modes:** off â run (10.4 km/h) â drive (48.6 km/h, red chip "áĄáá Âˇ ááááĽááá") â drive mode used to E2E the warning + auto-cancel live on the emulator.
- Higgsfield had 0 credits (free plan) so the "realistic pin" was hand-built as inline SVG â crisper at marker size anyway.

## Medi Run v33.0.1 â smooth puck, checkered pin, auto-finish

- **Smooth marker:** user puck now glides between GPS fixes via requestAnimationFrame lerp (950ms linear, matching the camera easeTo) with shortest-angle heading interpolation; jumps >120m snap (reroll/GPS teleport). Nav-app smooth.
- **Finish pin:** redesigned as a racing checkpoint â glossy amber teardrop, white core with 4Ă4 checkered-flag pattern (SVG clipPath), gloss highlight; captured â hue-rotates green (checker stays b/w).
- **Pin reached = session ends:** 'pin_reached' â green pop + banner "áááááŞáá â á áááá áĄá áŁáááááâŚ" â after 2s auto `finishRun()` â summary screen (confetti, stats). Timer no longer keeps running after capture. E2E verified with 1 áá target + run-sim (sim run speed bumped 2.9 â 4.6 m/s to keep dev demos under 4 min, still below the 25 km/h transport threshold).

## Medi Run v33.0.2 â solid lane, map theme toggle + auto day/night

- Removed the animated ant-line dashes (user found the animation choppy) â lane is now a clean solid teal stripe (glow + casing + fill).
- Map theme: `RunMap` takes optional `mapDark` prop overriding app theme; active screen defaults to time-based auto (night = 19:00â07:00, checked every minute) with a sun/moon HudRoundButton in the top chips row â manual tap wins for the session. Theme switches live via Mapbox Standard `lightPreset` (no reload).
- Fixed `formatPace` showing 3'60" (rounding bug â now rounds total seconds first).

## Home Medi Quest empty card (v33.0.3)

- Home was treating "no daily missions" (`priority.mode === 'empty'`) the same as "no quest profile" and replacing the whole card with `ááŚááĄ Medi áŻáá  áá  áááááááá áááĄáááá` â hiding level, rank, coins, streak.
- Hub already kept `QuestHubHeader` in that state. Home now matches: identity card stays; the empty sentence is only the Medi line under it.
- `homeModuleView` only returns `kind: 'empty'` when there is no profile. Unavailable + profile still shows stats.

## Medi Quest Phase 7 â Rewards Store (v34.0.0)

- Loyalty economy only (no cash/crypto/P2P). Server-authoritative redeem via `/api/rewards`; Coin ledger reuses `RewardLedger` with negative `REWARD_REDEMPTION` rows.
- Additive SQL: `server/prisma/phase7-rewards-store.sql`. Models: RewardDefinition/Redemption/Code/InventoryAdjustment/Partner/UserRewardEntitlement/Audit.
- ACTIVE seeds: `MEDI_THEME_7D` (300), `MEDI_PROFILE_STYLE_30D` (600) with real cosmetic entitlements (hub wash). DRAFT: Premium day/3d (no Quest Premium system), `PARTNER_TEST_10` (DEV only).
- Mobile: `/medi-quest/rewards`, `/:id`, `/mine`; Wallet âáááááá§ááá Medi Coinsâ; hub Gift tile. Spec asked 32.0.0 but app was already 33.0.3 â shipped **34.0.0**.
- Tests: 495 pass (incl. rewards concurrency/idempotency + COINS_EARNED redemption regression). Pixel 8 live UI QA not run this session â deploy SQL + restart API before device QA.

## Medi Quest Phase 7.1 â Rewards production readiness (v34.1.1)

- Neon: `prisma generate` + `db execute --file phase7-rewards-store.sql` (idempotent re-run OK). All 7 reward tables verified. Seed upsert 5 defs.
- Honesty: no full MediCard theme picker â renamed ACTIVE copy to Quest Style / Profile accent; distinct fulfillment (hub gold chip+border vs profile avatar gold frame). Premium + PARTNER_TEST remain DRAFT/hidden.
- Live E2E (`phase71-e2e-rewards.js`): redeem, idempotency, insufficient, lifetime vs balance, entitlement 7d/30d, period limit, max-1 profile, price snapshot.
- Pixel_8 QA (local APIâNeon): light/dark store/detail/mine/hub style/profile accent/wallet/offline/insufficient/large balance. Screenshots in `qa/phase-7.1/`.
- Hub entitlements now `useFocusEffect` so redeemâback shows Quest Style. Deploy doc: `server/docs/rewards-store-deploy.md`. Do not start partners/sponsored/social/admin.

## Medi Quest Phase 7.2 â Production deploy + wallet/coin consistency

- First push `454bfe5` auto-deployed but **update_failed**: `achievements.js` imported `emitAchievementClaimed` / `emitAchievementUnlocked` missing from committed `questRealtime.js`. Live stayed on `638bdbe` (`/api/rewards` 404).
- Hotfix `0328a76` exported those emitters â Render deploy **live**. Prod `/api/rewards` â 401 (auth), `/health` 200.
- Prod E2E (`server/scripts/phase72-prod-e2e.js` vs `https://medicard.ge`, QA `+995500000005`): ACTIVE catalog `MEDI_THEME_7D` + `MEDI_PROFILE_STYLE_30D` only; DRAFT direct redeem `REWARD_NOT_ACTIVE`; redeem + idempotency; entitlements; privacy 404 on other redemption; store balance matches Neon.
- Client (in `454bfe5`): `walletSourceLabel` (SYSTEM â  Mission), `invalidateMediCoinBalance` / `publishMediCoinBalance`; release = `prisma generate` + seed (no `db push`). App version **34.2.0**.
- Cross-device: no Store sockets; refresh on focus/foreground OK. Same-device: coin bus + authoritative redeem/claim balances.
- Freeze: Rewards Store v1 production routes live. Do not start Partner Rewards.

## Weather â Meteocons Lottie + page redesign (v34.2.1)

- Icons from [Meteocons](https://meteocons.com/) fill Lottie (`@meteocons/lottie` + `lottie-react-native` Expo 57 / 7.3.8).
- `Meteocon.tsx` statically requires a tight subset of fill JSONs; maps WMO conditions + air bands + utility slugs (thermometer, rain, wind, UV, sunset).
- Home emblem is now the animated icon inside the score ring (72px). Home stat tiles use small Meteocons.
- Weather detail page redesigned: mood-wash hero with 132px living icon, Medi advice card, air + today grid with animated icons, hourly/daily forecast cards.
- Respects reduced motion (first frame, no loop).

## Page transitions (v34.2.5)

- Root cause of the âlate popâ when changing pages: `enableScreens(false)` (added so the floating tab pill wouldnât get covered). JS views canât do native stack motion, so screens waited then snapped.
- Fix: native screens + `enableFreeze` back on; tab pill sits in `AppChromeOverlay` (iOS `FullWindowOverlay`, Android high-elevation sibling). Do not disable screens again.
- Shared `STACK_PUSH` (280ms slide) / `STACK_FADE` (tabs + run live). Tab bar fades instead of unmounting. Reduced motion uses a short fade.

## Medi Run target sheet (v34.2.7)

- AppChromeOverlay sits above RN Modal, so the floating tab pill covered **áááááĄ áááĄáá**. Sheet now calls `hideFloatingTabBar()` while open; pill hides instantly (no 220ms fade covering the CTA).
- **áĄáŽáá** is a full-width 56px row (label + input + áá/áááááŻá). Tapping the Georgian label focuses the field â it was a squeezed wrap chip and nearly untappable.

## Phase 8 â Partner Rewards Platform + Rewards Admin (v35.0.0)

- Additive schema: `RewardCampaign`, expanded `RewardPartner`, redemption snapshots (`campaignId`, `partnerIdSnapshot`, commercial minors), indexes. SQL: `server/prisma/phase8-partner-rewards.sql`. Deploy: `server/docs/phase8-partner-rewards-deploy.md`. No `db push`.
- Admin V2 tab **Rewards Operations**: Overview / Campaigns / Partners / Redemptions / Code Inventory (`server/admin/rewards-admin.js`). Capabilities via `Admin.capabilities` + `adminCapabilities.js` (null = legacy full access).
- APIs under `/api/admin/rewards/*`. Mobile store: partner display name on cards; visibility requires partner ACTIVE + live campaign when campaigns exist. First-party without campaigns unchanged.
- Code pool: import CSV/TXT, mask default, no plaintext in import report. Codes remain plaintext-at-rest (documented). No refunds / REWARD_REFUND in Phase 8 (safest default). No partner portal, sponsored quests, or reward pushes.
- Tests: 507 green (Phase 7 suite + `rewardsAdmin.test.js`). Production may ship with 0 partners/campaigns.

## Rewards: create partner was a silent prompt

- Button existed; click ran `window.prompt()` (easy to miss / blocked). Looked like "nothing happens".
- Replaced with a real drawer form (`POST /api/admin/rewards/partners`, DRAFT). Cache `rewards-admin.js?v=v35.5`.

## Admin sidebar restyle

- Groups no longer repeat the item name. Now áááááá á / áá áááŁáĽá˘á / áĄááĄá˘ááá.
- Uppercase is FiraGO OpenType `case` (same as landing `.mcb-column-inner h1.title`), not `text-transform` / Mtavruli. Latin still uses uppercase.
- Admin dates always Georgian: `5 áĄááĽá˘ááááá á, 2026` via `fmtDate` / `fmtDateShort` (Windows `ka-GE` was emitting "Sep").
- Cache `admin-v26.css?v=v26.5`, `admin.js?v=v26.6`.

## Admin user profile full width

- `#/users/:id` was capped at `max-width: 1120px` in `admin-v24.css`, so the profile sat in a narrow column with empty workspace on the right.
- Removed the cap. Overview is facts (fluid) + edit form (~420px). Account/activity grids, adoption tiles, devices, product rows, and audit use the extra width.
- Cache bump: `admin-v24.css?v=ops11`, `admin-v26.css?v=v26.3`. Verified at 1680px: `.user-page` fills the workspace minus the normal 28px gutter (1388 / 1444).

## Run hub polish + offline toast (v35.0.2)

- Target sheet: clip + transparent KAV so Android no longer paints a square white box behind the 28px radius.
- Hub hero: removed pin icon + pulsing rings. Background is `mobile/assets/run/runner-sprint.mp4` (desktop `0_Runner_Sprinting_1920x1080.mp4`).
- Offline: one shared ping, 2 failures required (was one 5s `/health` miss â full-width yellow banner pushing the whole app). Now a floating teal chip like Quest XP.

## Phase 8.1 â Partner Rewards production freeze

- Additive SQL re-applied (idempotent). Admin Georgian redesign + esc fix already live path.
- DEV partner E2E passed; fixture cleaned from Neon (partner/campaign counts 0).
- FIRST_WEEKLY + COMEBACK rarity corrected to UNCOMMON (rewards unchanged). Distribution now 14/14/11/8/3 vs original marketing table 13/14/10/8/5 â reported, not force-rewritten.
- Tests 510 green. Partner Rewards v1 FINAL-FROZEN.

## Achievement catalog integrity audit (post Phase 8 freeze)

- Explicit 50-key Phase 4 rarity list counts to **13/14/10/8/5** â the old marketing summary was **correct**; live/source had **6 wrong rarities**, not a bad aggregate table.
- Mismatches (rarity only; XP/coins/target matched): MOVE_100 RAREâEPIC, MOVE_250 EPICâLEGENDARY, WEEKLY_3 COMMONâUNCOMMON, WEEKLY_10 UNCOMMONâRARE, WEEKLY_25 RAREâEPIC, WEEKLY_52 EPICâLEGENDARY.
- FIRST_WEEKLY / COMEBACK already UNCOMMON â left alone.
- Source + hand-authored `CANONICAL_RARITY_BY_KEY` snapshot + Neon upsert synced. Ledger delta 0, userAchievement delta 0.
- Final distribution 13/14/10/8/5. Mobile patch **35.0.5** (rarity badge styling). No Partner Rewards / economy changes.

## Phase 9 â Medi Companion (v36.0.0)

- Emotional retention layer: Medi Home + Journey + Collection. No second XP/coins economy.
- Stage from UserQuestProfile level; Journey from QuestCompletion (daily=1, weekly=3); 25 milestones; mood deterministic (never clinical/guilt).
- Schema: MediCompanionProfile + MediJourneyUnlock (additive SQL applied on Neon). API `/api/medi-companion*`.
- Mobile: `/medi-companion`, home chip entry, SVG figure from robot-2, ka/en/fr/ru copy.
- Frozen systems untouched. Pixel light/dark device QA still pending (emulator boots aborted earlier).
- Deploy: `server/docs/phase9-medi-companion-deploy.md`.

## Phase 9.1 â Visual completion (v36.1.0)

- Explicit visualKey catalog: 27 ACTIVE cosmetics (2 defaults + 25 Journey), all unique + renderer-backed.
- Accents / poses / accessories / backgrounds / decorations composed in SVG (MediCompanionScene + Figure).
- Collection previews honest; offline equip disabled; multi-unlock aggregated toast + HTTP/socket dedupe.
- Journey hierarchy + between-threshold progress (units/nextAt) + completed-state copy.
- Tests: visual fulfillment, journey map, dedupe, economy/privacy regressions green.
- Pixel 8: auth/welcome light captures in `qa/phase-9.1/`; companion matrix incomplete (login timing + prior crash from SVG Path transform â fixed).

## Phase 9 ďż˝ Medi Companion mobile UI (v36.0.0)

- Routes: /medi-companion, /journey, /collection (Stack registered in root _layout).
- Lib: src/lib/companion/{api,cache,copy,devFixtures}; pi.mediCompanion + product events medi_companion_*.
- UI: SVG Medi figure from robot-2; home entry chip beside Quest; Talk ? /chat/doctor.
- Does not touch Rewards Store / Partner Rewards / Notification Brain / Quest economy / Achievement rewards.

## Phase 9.2 ďż˝ Companion live QA (v36.2.0) ďż˝ FINAL-FREEZE no

- Fixed worklet crash: moodTilt/pose offsets computed outside useAnimatedStyle (was `Tried to synchronously call a Remote Function`).
- Pose still via Animated.View only (no RN SVG Path/G rotation).
- Unique Journey cosmetic titles ka/en/fr/ru (cosmeticNames.ts); hub tile truncation polish; DEV fixtures MULTI_UNLOCK_1/4 + JOURNEY_COMPLETE + `?fixture=`.
- Pixel 8 authenticated session: Home ? Companion ? Journey ? Collection without redbox.
- Artifacts: `qa/phase-9.2/` (+ README). Multi-unlock 1 / 2ďż˝3 / 4+ toasts live; dark equipped scene; Talk to Medi.
- Server companion + quest/achievements/rewards tests green; cosmeticNames.test.js added to suite.
- FINAL-FREEZE **no**: Expo Go font_scale/wm-size reload blocked full 160% home + clean small Companion home; live QuestCompletion equip/dedupe E2E not fully re-run this pass.

## Phase 9.3 â Native QA closure (v36.2.0) â FINAL-FROZEN

- Native debug APK on Pixel_8 / Android 15 (JDK 17 rebuild); not Expo Go as SoT.
- Live equip PUT all 4 slots + DB `MediCompanionProfile.selectedCosmetics` + re-login persist; invalid PUT 400 unchanged.
- Live QuestCompletion: `AiInteraction` OK â `MEDI_USED` â units 23 â **MILESTONE_07**; dedupe `MILESTONE_07:2026-09-07T09:55:25.525Z`; second reconcile empty; cold start no toast.
- Native narrow (~360dp) + font 160/130 + dark+160 + reduced motion + offline/error fixtures captured in `qa/phase-9.3/`.
- Deep-link QA note: use `medicard:///medi-companion` (triple slash); double slash â Unmatched Route (in-app router.push OK).
- Runtime noise: expo-notifications missing sound LogBox (not Companion).
- Tests: companion + celebration dedupe + quest/rewards/achievements green; no product UI redesign this pass.
- **MEDI COMPANION V1 â FINAL-FROZEN**. STOP.

## App "stuck offline" after Phase 9.3

- Not prod down, not admin maintenance (`maintenanceMode` false; `https://medicard.ge/health` 200).
- Metro was started with local API (`http://10.0.2.2:4000`). `.env` later restored to medicard.ge, but `EXPO_PUBLIC_*` is baked at Metro boot â file change does nothing until restart.
- `useOffline` latches after 2 failed `/health` pings. Android: `Failed to connect to /10.0.2.2:4000`. iOS can never reach `10.0.2.2` (emulator-only alias) â stays offline.
- Fix: kill Metro, start with current `.env`, full reload. Phone + local API = LAN IP, never `10.0.2.2`.
- Shipped to `origin/main` as `82ab661` (Phase 9 Companion + Admin/Run). Metro restarted `--clear` with `EXPO_PUBLIC_API_URL=https://medicard.ge`. Android rebundled; no more `10.0.2.2` health fails. Prod `/api/medi-companion` still 404 until Render finishes.

## Partner activate "partner key invalid"

- Admin **ááááĽá˘ááŁá ááá** PATCHes `/partners/:id` with only `{ status: 'ACTIVE' }`.
- Campaign PATCH already merged the existing row; partner PATCH did not, so `upsertPartner` saw an empty key and threw `partner key invalid`. The key on the partner was fine.
- Fix: merge existing partner on PATCH (same as campaigns) + `upsertPartner` keeps key/contact fields on status-only updates. Restart API before retrying Activate.

## Campaign create was missing from admin UI

- კამპანიები tab only listed/activated. Empty copy described the pipeline but there was no create button (API `POST /campaigns` existed).
- Added **ახალი კამპანიის შექმნა** drawer: partner + new PARTNER_VOUCHER (or existing coupon/voucher) + campaign limits. Saves DRAFT. Then Codes import if CODE_POOL, then გააქტიურება.
- Do not attach campaigns to first-party `MEDI_THEME_7D` / profile style — that would hide them until a live campaign exists.
- Hard-refresh admin (`rewards-admin.js?v=v35.8`) + restart API.

## Admin V3 Step 1 — full UX/IA audit (no redesign)

- Existing admin is a hash SPA: `/admin` → `index.html` + `ops-center.js` + `admin.js` + `rewards-admin.js`. 13 nav screens. User investigation is already a **page**, not a drawer.
- Live QA at 1280/1440/1920: 0 JS errors, 0 failed requests, no horizontal overflow. Confirmed P0: campaign create modal footer off-screen; user Save below fold (not sticky).
- Do not invent Quest/Achievement/Companion admin — those have no admin UI today. Do not change KPI formulas until labels are honest (`გაგზავნილი` = scheduled; two meanings of `აქტიური`; rewards “today” is UTC).
- Step 2 start: shared chrome → Command Center attention-first → Rewards campaign as page + confirm activate. Docs: `docs/admin-v3-step1-audit.md`. Canvas: admin-v3-step1-audit.

## Admin V3 Step 2 — operator UX foundation (no page redesign)

- New layer last: `admin-v3.css` + `admin-v3.js` (`window.AdminV3`). Old CSS stays. Hash SPA unchanged.
- IA sidebar groups (existing screens only): Overview / People / Engagement / Health & Medi / Commerce / Operations / Production. Brand is MediCard / Admin “M”, not `+`. Rewards = gift, Quality = shield, Packages = layers.
- Shared primitives exist for later pages: header, section, toolbar, filter bar (same Tbilisi range + session persist), tabs, badges, buttons, fields, dialog, confirm, drawer adapter around `openDrawer`, sticky actions, toast, mutation helper, table/empty/error/skeleton, humanLabel, formatDate.
- Palette now includes ჯილდოები and is grouped by IA. Dirty guard on overlay close / ESC / nav; existing `confirm()` callers stay until page migration.
- Live QA `qa/admin-v3-step2/`: 0 pageerror, 0 console error, 0 HTTP 4xx/5xx, no overflow at 1280/1440/1920. Do **not** start Step 3 (Command Center hierarchy) until asked.

## Admin V3 Step 3 — Command Center (accepted to implement)

- `#/overview` only. Hierarchy: status → attention → today → Brain summary → movement → activity → retention → product signals.
- One resolver (`adminCommandCenter.js` + matching frontend). Any 24h AI error = attention. No fake health scores. 3 errors ≠ healthy.
- Feature `% აქტიურებიდან` is now intersection / AppActivity uniques (never >100%). Health inherits that honesty.
- Labels: აქტიური დღეს; ახალი is range-scoped in movement, today has ახალი დღეს; დაგეგმილი not გაგზავნილი; AI errors = last 24h wall-clock; retention = lifetime cohort.
- Requests: 7 → 6 (dropped `/analytics/medi`). Grain in URL. Live poll is `/system/health`; fail → დაგვიანებული / განახლება ვერ მოხერხდა, no toast spam.
- QA: `qa/admin-v3-step3/`. Live 2026-09-07: 3 AI errors / 9 requests, attention. Do **not** start Step 4 until asked. Recommend Users next.

## Admin V3 Step 3.1 — Command Center visual rescue

- CSS was loading (200, `text/css`). Ugly look = sparse V3 + legacy `.ops-page` / `.ops-range` fighting, not a missing stylesheet.
- New last layer: `command-center-v3.css` namespace `.v3-cc`. Shared tokens stay in `admin-v3.css`. Cache `v=v3.32`.
- Composition: toolbar → status → attention queue → today strip → Brain pipeline → activity+movement split → retention+signals split. Max-width 1320. 1280 keeps two columns (stack only ≤1100).
- Semantics unchanged. Only bar `width:%` remains inline.
- QA: `qa/admin-v3-step3.1/`. VISUAL ACCEPTANCE = yes. Recommend Users next. Do **not** implement Step 4 until asked.


## Admin V3 Step 4 � Users + full-width + section help (complete)

- Wired `admin-help-content.js` + `admin-users-v3.js` in `index.html` (v3.41).
- Workspace width: `--v3-page-max` ~calc(100vw - sidebar - gutters), caps ~1780?1840?2000; CC max 1680.
- Help: `AdminHelp` registry + `infoButton` / popover|dialog; overview + users keys; registry test.
- Users: toolbar filters, hash `q|status|package|activity|appVersion|page`, CSV parity via `buildAdminUsersWhere`, account vs activity wording (?????? ??????????), dense table.
- User page: shell header identity, summary grid, sticky save, Tbilisi package dates, destructive zone, runMutation/confirm.
- QA: `qa/admin-v3-step4/` 16 shots + `live-report.json`. VISUAL ACCEPTANCE YES.
- Step 5 next (do not implement): Push/Brain or Rewards � recommend Push/Brain first.

## Admin V3 Full Product Redesign

- Architecture: `server/admin/v3/shell.js` + `v3/modules/*.js` overrides; expanded `admin-help-content.js`; `v3/modules.css`.
- Fixes: SMS no dummy dest; Quality version coverage distinct-user semantics + test; Push default Brain + `?tab=`; Rewards URL tabs, full-page campaign create, confirm activate/pause, dialog code import; Settings sticky+confirm dangerous toggles; Audit `q` hash.
- QA: `qa/admin-v3-full-redesign/` (~59 shots). CSS prune deferred (documented).

## Admin V3 Final Unification (FINAL-FROZEN pass)

- Added `server/admin/v3/unify.css` � visual kill-switch for legacy card/KPI/phone DNA inside `.v3-module`.
- Push compose: compact notification preview + send summary (no decorative phone); inert hidden panels.
- Rewards overview: `v3-critical-strip` / `v3-panel` (no `ops-kpis`).
- Orders: vertical queue (`data-cols=1`, flex stack) instead of 4-col mosaic.
- Ops modules (packages/sms/pharmacy/medi/health) remapped panels/tables; Quality strips to V3 critical.
- Neutralized legacy `prompt()` code-import in `rewards-admin.js` (V3 dialog owns it).
- QA: `qa/admin-v3-final-unification/` contact sheets + recapture. Cache `?v=v3.60`.

## Medi Run v36.2.10 � active session persists + top badge

- Live run (running/paused) now persists to scoped prefs (`medicard.run.active.v1`); hydrate on login/cold start; wipe on finish/cancel/transport/new prepare.
- Leaving the map minimizes the session (chevron / Android back) � no longer forces finish. Finish only via Finish CTA (or pin auto-finish in store).
- Global `ActiveRunBadge` in AppChromeOverlay: pulsing live chip with clock + distance; tap ? `/run/active`. Hidden on the map itself. Summary auto-opens if pin finishes while map is closed.

## Home priority reorganization (v36.3.3) � order only

- No section redesign. Composition helper `mobile/src/lib/home/homeSectionOrder.ts` + `buildHomeSectionOrder`.
- AFTER order: Dashboard ? NextDose (self-hide) ? Quest+Companion chip ? Steps ? Hydration ? Weight ? Cycle (FEMALE) ? Lab ? Weather ? Run ? Symptom ? Analysis ? Consilium ? Recent ? Disclaimer.
- WHY: due meds before Quest; today metrics before Weather/Run; Cycle in health context; Symptom/discovery after wellness/engagement; Rewards still Quest-hub only.
- Tests: `npm run test:home` (8 pass). Screenshots blocked (no adb device).

## Home reorganization 36.3.4 closure

- Quest reclassified as MEDI_ENGAGEMENT; moved AFTER Steps/Hydration/Weight (health-first).
- Deleted unused HomeStartSection.
- NextDose: keep live self-hide (avoids duplicate useMedications fetch).
- Hydration chip vs section: intentional (summary vs actionable).
- Native QA on Pixel_8 emulator: top = Dashboard+Steps+Hydration; scroll = Weight then Quest; Lab/Weather/Run/Symptom after. Artifacts: `qa/home-reorder-36.3.4/`.
- Tests: `npm run test:home` 10 pass.

## Admin sidebar redesign (v3.61)

- Brand: mobile Medicard logo mark (SVG) on teal tile + Georgian **მედიქარდი** with FiraGO `case` only on the logo; subtitle ადმინი.
- Menu sections: no uppercase / no `case` feature; larger (13px groups, 14px / 44px nav rows); sidebar width 272px.
- Section labels Georgian: მიმოხილვა / ადამიანები / ჩართულობა / ჯანმრთელობა / კომერცია / ოპერაციები / სისტემა.
- Cache `admin-v3.css` + `v3/modules.css` + `v3/unify.css` `?v=v3.61`. Hard-refresh `/admin`.

## Admin Command Center redesign (v3.62)

- Full-width overview (no 1680px cap).
- Live hero: **ახლა აპში** (`onlineNow` via Socket.IO `ops:live`, AppActivity ≤90s) + აქტიური/ახალი დღეს + სულ + AI errors.
- Infra strip: mobile app version + gates, OpenRouter USD, EvidenceMD usage, Medi 24h.
- Taller activity chart + growth (new users) chart; Brain funnel bars; retention bars.
- `patchOpsLive` now patches `onlineNow` + caches last snap for first paint after render.
- Help keys: `overview.live`, `overview.infra`, `overview.growth`. Cache `?v=v3.62` (+ `ops-center.js?v=v26.10`).

## Admin CC polish (v3.63)

- Status + attention redesigned: icon tiles, pill chips (no hover on chips), alert cards with proper hover (no flat list glitch).
- Icons across hero / infra / funnel / retention / features.
- Cache `?v=v3.63`.

## Admin Users registry redesign (v3.64)

- `#/users` full redesign in `admin-users-v3.js` + `.v3-users` CSS: KPI strip (სულ / აქტიური / დაბლოკილი / ULTIMATE / ახალი 7დ), chip filters (status / package / activity), board card, richer sortable table (პირი · ანგარიში · აქტივობა+feature chips · პაკეტი+quota · აპი · რეგისტრაცია), numbered pager, 20/page.
- KPI click = filter; CSV + reload in header; privacy-safe (no chat/med/health).
- Full-width board (`max-width: none`). Cache `?v=v3.64`, `admin.js?v=v27.6` (date sort for lastActiveAt/createdAt).

## Admin User page redesign (v3.65)

- `#/users/:id` hero (avatar, name, status/package, copy ID, platform/app) + KPI strip (აქტივობა / AI / პროდუქტი / შეტყობინება / ჩანაწერები).
- Investigation tabs restyled inside `.v3-user-board`; edit rail + sticky save kept.
- Fixed race crash: list `paintTable`/`paintUsers` set `user-meta`/`page-ind` after navigating to detail → null textContent. Now guarded.
- Cache `?v=v3.65`, `admin.js?v=v27.7`.

## Admin user page polish (v3.66)

- `შეტყობინების ქცევა` / `.inv-fatigue`: one segmented strip (no double borders from legacy + v3).
- `ანგარიშის რედაქტირება`: dropped `umodal-form` chaos; clean 2-col symmetric grid with equal field heights.
- Cache `?v=v3.66`.

## Admin user page polish (v3.67)

- Full-width `#/users/:id` (no page max).
- `პროდუქტის ათვისება`: proper 4-col adopt cards with icon tiles (legacy flex/grid fight fixed).
- Sticky უკან/შენახვა: `position: fixed` dock under content (no longer rises mid-scroll).
- Icons on tabs, section titles, fact rows, fatigue/brain boxes, devices.
- Cache `?v=v3.67`, `admin.js?v=v27.8`.

## Admin user page polish (v3.68)

- Product usage rows: aligned 4-col table (მოდული · სტატუსი · ბოლო გამოყენება · პერიოდი); unused = **გამოუყენებელი** red badge.
- Decisions table: scroll + 10/page pagination.
- Page header matches sidebar brand height (40px mark tile, 64px bar, bottom border).
- Cache `?v=v3.68`, `admin.js?v=v27.9`.

## Admin chrome lane align (v3.69)

- Shared tokens `--v3-chrome-*`: sidebar brand + page header bottom borders share one Y lane (18+6+40+18).
- Header mark matches brand (solid teal tile); title/subtitle match brand type scale.
- Workspace `padding-top: 0` so header owns top chrome. Cache `?v=v3.69`.
