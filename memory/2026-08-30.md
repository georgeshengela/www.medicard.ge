# 2026-08-30

Cycle module visual system brought onto the rest of the app. User liked period/phase colors (rose / purple) but not the pink-era chrome (blobs, cream page, glass header, rose gradients, 800 system weights, 28px cards).

Locked: page `bg100`, cards `surface` + `bg300` hairline, no shadows, CTAs teal / `#0D9488`, type Noto, section titles via `HomeSectionTitle` outside cards, radius 16. Period `#E11D48` / `#FB7185`, fertile `#C026D3` / `#E879F9` stay on data only.

Expo version **11.0.2**. Cycle hub middle ring is the Nightingale score horseshoe from Figma `9001:283189` / `9001:295435` — exact paths, rose fill, dotted segments, white knob, Medicard mark. Copy is cycle day (`ციკლის დღე` / `{n}-დან`), never Nightingale.

AI რჩევები შენთვის redesigned as a real visual module (11.0.3): teal/phase wash + rings + Medi mark, filled CTA, day/phase chips, then a horizontal companion-card rail. Title stays outside. Advice still depends on phase / mode / log / conditions.

Cycle production-quality work starts with a spec only (no code): `docs/CYCLE_PRODUCT_SPEC.md`. Server `cycle.js` is the intended canonical engine; client `cyclePhase.ts` must stop computing medical values. Forecast today uses stored avg 28, not inferred. Partner share was public (Phase 0 audit); Phase 2 closed that. Remaining open decisions: timezone/today, offline, privacyEnabled vs lock, client cyclePhase leftover, dual log UI, contraception, PMS day mapping.

Phase 1 implemented: canonical server engine + Tbilisi today + averages.source (C) + periodRanges. Client render paths read `CycleBundle`. Expo version **12.0.0**. Decision #1 locked to C.

Phase 2 implemented: partner share is no longer public. JWT required; 64-hex CSPRNG token hashed at rest; 30-day expiry; owner revoke; permissions `period` / `cyclePhase` / `fertileWindow` / `symptoms`. Partner route `/share/cycle/[code]` outside the cycle biometric lock. Expo **12.0.1**.

Phase 3 implemented: period ranges stay inferred from CycleLog bleed (`light|medium|heavy`). Spotting is not a period day. `PUT /api/cycle/period` start/end/fill. History UI expands to per-day edit/delete/add. No Period table. Expo **12.0.4**. Do not start Phase 4.

Phase 3.1: End Period no longer backfills `medium` on unlogged days. It only clears bleed after the selected day. Schema cannot represent unknown flow intensity. Historical `fill` stays explicit and requires confirmation. Expo **12.0.5**.

Phase 4: Cycle UX only — hero + confidence, informational ring, calendar/strip distinction, one logging architecture (quick subset of full), settings sections, a11y, Cycle TypeScript tokens. Expo **13.0.0**. Do not start Phase 5.

Phase 5: medical honesty / estimate language across Cycle UI, notifications, AI prompt, report, ICS, partner, home. Engine math unchanged. Expo **13.0.1**. Do not start Phase 6.

Phase 6: Cycle offline reliability — per-user canonical bundle cache + mutation queue. Client does not recompute predictions. Partner peek stays online-only. Expo **14.0.0**.

Phase 6.1: native Cycle offline state is AES-256-GCM (Web Crypto) with a SecureStore DEK. Web does not durably persist Cycle health payloads. Start Period is one mutation. Quota 429 is permanent; rate-limit 429 uses Retry-After. Expo **14.0.1**.

Phase 7: TTC fertility tracking completeness. `CycleLog` gained nullable `ovulationTest` / `pregnancyTest` (`negative|positive|unclear`). Observations stay user-logged. Positive OPK does not change ovulation/fertile-window math. Positive pregnancy test does not auto-switch mode — it offers the existing pregnancy transition. Partner share does not include OPK, pregnancy tests, BBT, mucus, or intercourse. HealthKit/Health Connect still do not map OPK. Expo **15.0.0**. Do not start Phase 8.

Expo Go last-period date save kept showing `დაფიქსირდა შეცდომა` after the Render deploy (Cursor web worked). Cause: Expo/Android Modal + PATCH body, not the production API being down. Fix in **15.0.5**: inline calendar (no Modal) on native, `POST /api/cycle/last-period` + `PUT /api/cycle/profile`, loadBundle failure after write still returns 200. User confirmed Expo date save works after this deploy.

Phase 8 implemented: contraception profile + server prediction-availability layer. Expo **16.0.0**.

Phase 9 implemented: structured pain, lifestyle, custom tags, journal history. Expo **17.0.0**. Observations do not change the engine.

Phase 10 implemented: historical analytics from actual period boundaries. PMS heatmap is days-before next logged period. Expo **18.0.0**. Prediction engine unchanged. Do not start Phase 11.
