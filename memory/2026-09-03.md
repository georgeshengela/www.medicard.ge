Landing polish: one sticky phone theater on a 1504px canvas. Screens crossfade from scroll, dock, day timeline, and feature cards. No extra per-section mockups. No CSS gradients. Light/dark switch kept.

Later: iPhone 16 Pro vector frame (`/iphone-16-pro.svg`) — titanium bezel, action/volume/power/camera-control, no island overlay, no aura/shadow/tilt. Nightingale Regular icons via CSS mask at 18/24/14px. Invalid nested `<g>` in some SVGs broke masks; close groups and use `#000` fills.

Hero right column now iPhone 15 Pro Max chrome (`/iphone-15-pro-max.svg`), screen locked to native 1170×2532, flush to wrap right. Under-phone control is a bare stack: name, 8 round icon tabs, counter — no card. Phone scale capped at 0.92.

Mobile: phone sits after hero copy (not after all chapters), not sticky, ~0.58 scale. Header is logo + theme + menu; download lives in the drawer. Shipped `f97d06e` to `main` for Render / medicard.ge.
