# 2026-09-10

## Cycle Phase 34 — owner-opted prenatal care OS calendar export (57.0.0)

One-way local-device export of a user-planned care date. Explicit **კალენდარში დამატება**. No catalog-window events. All-day, no invented 09:00, generic title `Medicard — დაგეგმილი ვიზიტი` by default, no private note, no Medicard OS alarms. Ownership is device-local (`medicard.pregnancy.careCalendar.v1.{userId}`), not `PregnancyCarePlanItemState`. Expo Calendar via `expo-calendar/legacy` (Next API is stubbed in Expo Go). App **57.0.0**.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase34-care-calendar/01-planned-date-calendar-action.png` … `12-small-screen.png`. Native provider proof in `native-events.txt` (create all-day 2026-09-19, update to 2026-09-22 same `_id=1`, delete → no rows; `hasAlarm=0`; no `SECRET_PLANNER_NOTE`). Device restored (font 1.0, night no, wm size reset). iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PREGNANCY_CARE_CALENDAR_EXPORT_CONTRACT.md`.

## Cycle Phase 33 — user-opted prenatal care reminders (56.0.0)

User-planned-date reminders only. Explicit opt-in (`შემახსენე`), default OFF. Catalog windows never create candidates. Notification Brain is the sole delivery authority (`pregnancy_care_plan` / `pregnancyCareReminder`, prefix `pregnancy_care:`). Offsets 0/1/3 civil days, 09:00 + quiet bump. Send-time revalidation. Partner / AI / doctor / Home / Calendar / Journal / Quick Log unchanged. Export includes `reminderEnabled` + `reminderOffset`. App **56.0.0**.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase33-care-reminders/01-planned-no-reminder.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset, airplane off). Seed left on **on**. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PREGNANCY_CARE_REMINDER_CONTRACT.md`.

## Cycle Phase 32 — pregnancy prenatal care planner (55.0.0)

Source-backed informational + organizational prenatal care planner. Not medical orders, diagnosis, risk scoring, medication, AI, or a second GA/due-date engine. Catalog `prenatal-care-v1` (WHO/NHS/NICE/ACOG, reviewed 2026-09-10). User states episode-scoped in `PregnancyCarePlanItemState`. Compact `carePlannerSummary` on `GET /api/cycle/pregnancy`; full `GET/PUT /api/cycle/pregnancy/care-plan`. Capability `showPregnancyCarePlanner` derived from Phase 27 `showPregnancyOverview`. Partner / doctor summary / AI / Notification Brain / Home / Calendar / Journal / Quick Log unchanged. Writes online-required; catalog readable offline. App **55.0.0**.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase32-care-planner/01-care-plan-overview-card.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset, airplane off). Seed left on **t2** (week 20). iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contracts: `docs/PREGNANCY_CARE_PLANNER_CONTRACT.md`, `docs/PREGNANCY_CARE_PLANNER_DATA.md`.

## Cycle Phase 31 — observation data transparency & comparison explainability (54.0.0)

Transparent explanations of why Pregnancy/Peri observation rates, two-window numbers, or HIGHER/LOWER labels appear or do not. Server-owned reason enums; mobile does not recompute thresholds. No coaching, streaks, nudges, diagnosis, AI, Notification Brain, doctor/partner leak, or math change. App **54.0.0**.

Canonical helpers: `server/src/lib/cycleObservationExplainability.js` + `CycleObservationExplainSheet`. Seed: `server/scripts/cycle-phase31-qa-seed.js`. Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase31-explainability/01-pregnancy-rate-explanation.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset). Seed left on **peri-lower**. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_OBSERVATION_EXPLAINABILITY_CONTRACT.md`.

## Cycle Phase 30 — two-window exposure-aware observation comparison (53.0.0)

Descriptive comparison of two adjacent non-overlapping 14-day civil windows. Reuses Phase 28 `aggregateObservationExposure` (no second denominator). Registry `exposureComparisonEligible` (default false; requires assessment + rate). Pregnancy: nausea / vomiting / fatigue ∩ ACTIVE episode. Peri: hot_flashes / night_sweats / fatigue, last 30 civil days, no episode/progression language. Thresholds: assessed≥5/window, coverage≥35%, availableDays≥7/window; direction needs ≥20 points AND 1.5× relative (zero-baseline: recent present≥3 and ≥25 points; coverage ratio ≤2×). Numbers vs direction are separate. No SIMILAR / improving / trending / hormones / diagnosis / AI / doctor / partner / Notification Brain. Max 3 rows after existing Phase 23/26 ranking. App **53.0.0**.

Canonical helper: `server/src/lib/cycleObservationExposureComparison.js`. Seed: `server/scripts/cycle-phase30-qa-seed.js`. Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase30-exposure-comparison/01-pregnancy-no-comparison.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset, airplane off). Seed left on **pregnancy-cleared**. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_OBSERVATION_EXPOSURE_COMPARISON_CONTRACT.md`.

## Cycle Phase 29 — exposure-aware observation rates (52.0.0)

First allowed denominator: field-specific `assessedDays` (PRESENT + explicit ABSENT). UNKNOWN excluded. Optional `exposure` on Pregnancy/Peri Journal **expanded** rows only. Thresholds: present≥2, assessed≥5, coverage≥30% of availableDays. Whole-percent rounding. No 0% cards, no rate after one occurrence, no calendar/log/mode-day denominators, no directional claims, no diagnosis, no OpenRouter. Pregnancy = ACTIVE episode ∩ last 30 civil days. Peri: last 30 civil days. Server helper `isExposureRateDisplayEligible` / `aggregateObservationExposure`. Seed: `server/scripts/cycle-phase29-qa-seed.js`.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase29-exposure-rates/01-pregnancy-occurrence-only.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset, airplane off). iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_OBSERVATION_EXPOSURE_RATES_CONTRACT.md`.

## Cycle Phase 28 — explicit observation assessment & exposure foundation (51.0.0)

Denominator infrastructure only. Sibling JSON `CycleLog.observationAssessments` stores explicit ABSENT; PRESENT is derived from `symptoms[]`. Historical missing and historical `false` stay UNKNOWN. Pregnancy/Peri optional three-state editor (უპასუხო / დღეს იყო / დღეს არ იყო). No percentages, no directional claims, no diagnosis, no OpenRouter. Partner / AI / doctor / Notification Brain unchanged. Personal export includes owner assessments.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase28-observation-assessment/01-pregnancy-assessment-empty.png` … `12-small-screen.png`. Seed: `server/scripts/cycle-phase28-qa-seed.js`. Device restored (font 1.0, night no, wm size reset, airplane off). iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_OBSERVATION_ASSESSMENT_CONTRACT.md`.

## Cycle Phase 27 — mode capability & presentation integrity (50.0.0)

Hardening only. One canonical presentation matrix (`cycleModeCapabilityMatrix.js` on server and mobile, duplicated; no Metro shared package). Presentation screens ask `cycleModeCapabilities` / `supportsCycleCapability`. Domain / transition / doctor / notification / pregnancy-episode checks stay direct. Contract: `docs/CYCLE_MODE_CAPABILITIES_CONTRACT.md`.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase27-mode-capabilities/01-track-overview.png` … `12-small-screen-shared-chrome.png`. Seed left on **TRACK_PERIOD** (phase 25 `track` after peri rich logs). Device restored (font 1.0, night no, wm size reset). iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`.

## Cycle Phase 26 — denominator-safe Perimenopause observation summaries (49.0.2)

Dedicated aggregator `server/src/lib/cyclePerimenopauseObservationTrends.js` attached on existing `GET /api/cycle` → `perimenopause.observationSummaries`. Journal-only, current `PERIMENOPAUSE` mode, last 30 civil days (90-day fetch cap from the existing bundle). Threshold 2. No percentages, calendar denominators, episode model, mode history, diagnosis, hormones, or OpenRouter. Registry flag `perimenopauseSummaryEligible` defaults false. `vaginal_dryness` and fertility/sexual/notes stay out.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase26-peri-observation-summaries/01-peri-summary-empty.png` … `12-small-screen.png`. Seed: `server/scripts/cycle-phase26-qa-seed.js`. Left on **expanded**. Device restored (font 1.0, night no, wm size reset). iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PERIMENOPAUSE_OBSERVATION_SUMMARIES_CONTRACT.md`.

## Cycle Phase 25 — doctor-summary Perimenopause tracking context (48.0.0)

Additive clinician header when current mode is `PERIMENOPAUSE`. Wording is user-selected tracking mode, not a diagnosis. Optional Phase 24 variability (24–46 / 4 intervals) when ≥2 completed intervals. Same `GET /api/cycle/doctor-summary`. Pregnancy Phase 20 unchanged. No AI / partner / analytics / engine / schema.

Android 12 shots captured on Pixel_8 (`emulator-5554`): `qa/cycle-phase25-doctor-perimenopause/01-peri-summary-ka.png` … `12-small-screen-fr.png`. HTML: `00-report-{ka,en,fr,ru}-peri.html` plus TRACK/Pregnancy controls. Seed left on Phase 24 `rich` / `PERIMENOPAUSE`. Device restored (font 1.0, night no, wm size reset). iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Seed: `server/scripts/cycle-phase25-qa-seed.js` (`peri|track|pregnancy`).

## Cycle Phase 24 — Perimenopause mode foundation (47.0.0)

Explicit user-selected `PERIMENOPAUSE` tracking mode. Not a diagnosis. No age/symptom auto-entry, no 12-month menopause confirmation, no OpenRouter. Forecast arithmetic unchanged; late/fertile presentation suppressed via capabilities. Variability = last 6 completed intervals, unclamped (fixture 24–46). Reused `hot_flashes` / `night_sweats`; `vaginal_dryness` stays PRIVATE and off Overview recents.

Android 12 shots: `qa/cycle-phase24-perimenopause/`. Contract: `docs/CYCLE_PERIMENOPAUSE_CONTRACT.md`. iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Seed: `server/scripts/cycle-phase24-qa-seed.js` (`rich|empty|low`). Left on **rich** after screenshots.

## Cycle Phase 23 — denominator-safe pregnancy observation trends (46.0.0)

Server-authoritative occurrence summaries on the current ACTIVE pregnancy episode only. Journal section `ორსულობის ჩანაწერების შეჯამება`. No percentages, no calendar-day denominators, no diagnosis, no OpenRouter. Android 12 shots: `qa/cycle-phase23-pregnancy-observation-trends/`. iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Seed: `server/scripts/cycle-phase23-qa-seed.js` (`empty|two|rich|isolation`). Left on isolation fixture after screenshots.

## Cycle Phase 22 — pregnancy symptom & body-changes (45.0.0)

Observation-first Pregnancy logging on canonical CycleLog. No diagnosis, triage, OpenRouter, or parallel store. Episode-scoped `todayObservations` / `recentObservations` on GET `/api/cycle/pregnancy`. New key: `heartburn` (BOOLEAN, deny defaults). Android 12 shots: `qa/cycle-phase22-pregnancy-observations/`. iOS deferred.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Seed: `server/scripts/cycle-phase22-qa-seed.js` (week 18, episode startedAt today−40).

## Cycle Phase 21 — pregnancy timeline & milestones (44.0.0)

Educational calendar journey when mode is PREGNANCY and an ACTIVE episode has valid dating. Server `timeline` on GET `/api/cycle/pregnancy`. Trimester = existing Phase 18 bands (T1 0–12, T2 13–26, T3 27+). No client gestational math. Phase 19 catalog unchanged. No AI / partner / doctor-summary / Notification Brain.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Left on week-18 seed after screenshots.

Android 12 shots: `qa/cycle-phase21-pregnancy-timeline/`. iOS deferred.

## Cycle Phase 20 — doctor-summary Pregnancy header (43.0.0)

Additive clinician header on the existing Cycle doctor summary when profile mode is `PREGNANCY` and there is a valid ACTIVE episode. Uses `presentPregnancyDating` (Phase 18). No fruit, no diagnosis, no AI, no partner leak.

QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Restored to week-8 seed after review-required screenshots.

Android 12 shots + HTML reports: `qa/cycle-phase20-doctor-pregnancy/`. iOS deferred.
