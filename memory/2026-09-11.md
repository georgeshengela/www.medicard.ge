# 2026-09-11

## Website slogan is the new one

Landing hero, privacy, and terms now use **ყველაფერი შენი ჯანმრთელობისთვის.** Old three-liner (`შენი ჯანმრთელობა. შენი ისტორია. შენთან ყოველთვის.`) is off the public site. Rebuild legal with `node scripts/build-legal-pages.mjs`. In-app privacy footer aligned. Needs a server deploy for medicard.ge.

## Hub CTA მენსტრუაციის დასრულება was a no-op

`planEndPeriod` only cleared bleed *after* `endDate`. The hero always sends today, and future days cannot be logged, so confirm did nothing. End now clears the stop date (today is no longer a period day), overlay matches, long logged runs are endable. Version **1.0.0.7.68**.

## RA-01 auth / session / private files FINAL-FROZEN

Canonical artifacts: `qa/release-audit/RA-01/`. RA00-001 reproduced (unauth GET `/uploads` → 200 + bytes + 7-day public cache) then closed: deny stub + `GET /api/files/:filename` owner auth, private no-store, traversal blocked, live IDOR 404, unlink on delete. AppSettings had `qaOtpEnabled=true`; production NODE_ENV now hard-disables master OTP. Canonical unique routes **215**, UNKNOWN **0**. Android emulator-5554: logout to welcome, login UI, logged-out cycle deeplink gated. Maestro CLI absent. Version **1.0.0.7.67**. Next: RA-02 client crash P1s (RA00-002/003/004). Do not start it.

## RA-00 full product inventory (discovery only)

Canonical artifacts: `qa/release-audit/RA-00/`. Counts from generated inventories: 114 mobile screens, 15 admin hashes, 811 master-control rows, 207 API paths in JSON (live stack ~217), 69 Prisma models, 13 AI pipelines, 113 test files. Server unit suite 1565 pass / 0 fail. Mobile engage 1 fail. `tsc` 123 errors. Product logic not changed. P0: unauthenticated `GET /uploads/*`. Do not claim the app is tested. Next execution phase is RA-01 (auth + uploads boundary) — not started.

## Android visual QA (Georgian) — not frozen

Real Expo Go on Pixel_8 `emulator-5554` (Android 15 / API 35, 1080×2400 @ 420). Artifacts: `qa-artifacts/georgian-localization/android/1.0.66/`. Opened auth, home, lab, symptoms, chat, meds, profile, location onboarding, Cycle hub/settings/summary, Quest, Medi Run. Compact `1080x1920` rendered. **Font 1.3× not rendered** (Expo Go splash/reload). Assessment preview spinner. Pregnancy/postpartum/patch/log not fully on this TRACK session. Copy fixes: `ka.chat.writeMessage` → `დაწერე შეტყობინება…`; `ka.modules.lab.title` → `გაშიფრეთ ანალიზები`; OTP suffix `მისამართზე ${email}` (after-shot not isolated). Profile shows **1.0.0.7.66** (QA brief asked 1.0.66). Conclusion: localization remains open.

## Public version identity is five-part 1.0.0.7.66

User wanted Instagram-style `G.0.0.B.R` as the face of the app, starting here. `expo.version` is `1.0.0.7.66` (Android versionName, profile wordmark, `/api/app/status`). Apple-safe `expo.ios.version` is `1.7.66` plus plugin last-write guard. Never use `1.7.66` as `minAppVersion`. EAS remote native counters are **Android 2 / iOS 2** (local 66 is ignored). First production EAS build autoIncrements to 3 unless remote is raised. App unpublished. No TestFlight — preview-build install only. Profile About is a compact version row.

## Release blockers: Medi-is-not-a-doctor, completeness metric, store version (1.0.66)

User-facing `AI ექიმი` removed. Symptom composer `სიზუსტე` → `მონაცემების სისრულე` (input completeness, not accuracy). `expo.version` is three-part **1.0.66**; internal ledger **1.0.0.7.66** in `extra.medicardInternalVersion`. Expo visual QA still not run.

## Georgian source-copy cleanup before Expo visual QA (1.0.0.7.66)

Focused voice/clarity pass only. Did not repeat the 3,562-entry audit. Version `64.0.1` → `65.0.3` was **not** this cleanup: HEAD is `39.0.22`; dirty Cycle work plus earlier Georgian passes remapped the public line to five-part `1.0.0.7.65`. This cleanup bumps **1.0.0.7.65 → 1.0.0.7.66**. Localization **not frozen**. Expo not run.

## Store version format (1.0.0.7.65)

User-facing `expo.version` is Instagram/Facebook five-part (`G.0.0.B.R`). Current **1.0.0.7.65** = generation 1, 7 store trains, 65 small revisions. Internal 3-part ledger stopped at 65.0.3. Parser treats 5-part as a newer epoch so Brain/outcome gates (`23.0.3` / `24.0.0`) still pass. Cycle phase numbers stay in docs/QA.

## Georgian localization Phase 4 editorial (not frozen)

Native Georgian sentence-level pass on `ka.ts`, satellites, location permission strings, and duplicated Quest/server error copy. Confirmed defects (name hint, lab align, interactions, privacy, weight pace, observation wording, ovulation verb, contraception card, first visit, upload, location, AI default, sync terms, notification frequency, Medi Run) corrected with consumer context. Cycle leftover formal `-ეთ` / `თქვენ` in the same journeys converted to direct. App version **65.0.3**. Localization remains **not frozen**. Expo not run this pass.

## Georgian localization Phase 3 (not frozen)

Contextual trace of **543** keys added in `ka.ts` vs HEAD `e518f9a` (artifact `qa/georgian-l10n-phase3/ka-added-keys.md`). Auth got formal `ka.auth.retry` / `ka.auth.networkError`; shared `ka.common.retry` stays direct. Hydration 1–2 is goal-progress, not diagnosis. Android visual QA on Pixel_8 `emulator-5554` 1080×2400 @ 420 plus compact 1080×1920. Assessment name/PCOS, pregnancy first-visit, contraceptive patch picker, and the auth network banner were **not** visually verified. App version **65.0.2**. Localization remains **not frozen**.

## Cycle Phase 42 gauge P1 closure (65.0.1)

While `forecastEligibility.allowed === false` after POSTPARTUM → TRACK, the gauge still painted stored/default length via `usedCycleLength` → `CycleStatusGauge` (`${n}-დან`, progress = day/length, `|| 28`). Presentation-only: `suppressCycleLengthChrome` = `!forecastPresentationAllowed`. Gated TRACK/TTC/Home show `ციკლის ახალი ისტორია გროვდება` + `—`, no denominator/ring. Ready + ordinary TRACK unchanged. Engine / eligibility semantics / AI / Brain untouched. Android recapture `01-gated-track-no-28` … `12-small-screen`. Small-screen ready status now wraps (shorter gauge when `screenH < 720`). Device restored. Seed left on **three** / TRACK_PERIOD. iOS deferred.

## Cycle Phase 42 — explicit postpartum return to TRACK (65.0.0)

Owner-initiated POSTPARTUM → `TRACK_PERIOD`. Mode transition ≠ forecast readiness. Additive `forecastGateKind` / `forecastGateEpisodeId` (never backfilled). Readiness = two valid 18–45 day intervals from OWNER + MENSTRUAL_PERIOD starts on the exited episode (`inferCycleStats` `gaps.length >= 2`). Default 28, stored average, pre-pregnancy cycles, unclassified bleeds, and other episodes do not satisfy. Server `forecastEligibility` is authoritative; pending ≠ allowed. GET `/api/cycle` profile omits gate fields; owner export may include `postpartumReturn`.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase42-postpartum-return-to-track/01-postpartum-return-action.png` … `12-small-screen.png`. Live GET traces in `forecast-eligibility-traces.json`. Device restored (font 1.0, night no, wm size reset). Seed left on **three** / TRACK_PERIOD. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_POSTPARTUM_RETURN_TO_TRACK_CONTRACT.md`.

P0 during QA: GET `/api/cycle` `profile` still spread the raw Prisma row (episode id leak) even though `omitForecastGateFromProfile` existed — fixed to use `profileView`. ConfidenceHint was showing “რამდენიმე ციკლის მიხედვით” while gated — hidden with `hidePredicted`. Remaining P1 (stored `28-დან` while gated) closed in 65.0.1.

## Georgian copy audit (64.0.1)

Product-wide Georgian quality pass. Source of truth remains `mobile/src/i18n/ka.ts` plus satellite catalogs. Glossary: `docs/GEORGIAN_STYLE_GUIDE.md`. Medical meaning preserved. Phase 40/41 postpartum TRACK empty strings left frozen. Push templates kept in lockstep (`pushCopy.ts` + `pushTemplates.js`). Legal HTML rebuilt from `scripts/privacy-source.md`.

## Cycle Phase 41 — owner-classified return of period (64.0.0)

POSTPARTUM owner may mark a bleed episode as their period (`ეს ჩემი მენსტრუაცია იყო`). Default remains unknown bleeding. Additive `CyclePostpartumBleedClassification` (`MENSTRUAL_PERIOD` / `OWNER` only). Episode identity is postpartum episode + bleed run start from frozen `inferCycleStats`. Unclassified postpartum flow stays out of `engineLogWhere`. Classified PERIOD_FLOW days may join factual menstrual history; POSTPARTUM forecast/late/fertile/ovulation stay suppressed. No auto mode switch. No fertility/ovulation/return-of-period inference. Phase 38 AI fail-closed, Phase 39 doctor shape, Phase 40 copy heading, partner, Brain unchanged.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase41-postpartum-period-classification/01-postpartum-bleed-neutral.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset). Seed left on **classified** / POSTPARTUM. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_POSTPARTUM_PERIOD_CLASSIFICATION_CONTRACT.md`.

## Cycle Phase 35 — owner-entered prenatal appointment clock time (58.0.0)

Optional `plannedTime` (`HH:mm`) on an already-planned prenatal care date. Not a medical scheduler. Never invents 09:00/10:00/12:00. Date-only remains the default. Clearing the date also clears time. Writes stay atomic on existing PUT `/api/cycle/pregnancy/care-plan/:careItemId`.

Calendar: date-only still all-day (Phase 34). Timed export uses device-local start + 30-minute technical end (not shown as appointment length). Adding/removing/changing time never silently mutates the OS event — explicit განახლება, same owned `_id`. No OS alarms. Phase 33 reminders still date-based at 09:00 and ignore `plannedTime`.

Today + future clock: new timed export allowed. Today + past clock: new V1 export suppressed. Past civil date: still suppressed.

Owner-only. Personal export includes `plannedTime`. Partner / doctor summary / Medi unchanged. Time UI only in care-item detail.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase35-appointment-time/01-date-only-no-time.png` … `12-small-screen.png`. Native provider proof: `native-proof.txt` (same `_id=2` all-day → timed → all-day; `hasAlarm=0`; no `SECRET_PLANNER_NOTE`). Device restored (font 1.0, night no, wm size reset). iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PREGNANCY_CARE_APPOINTMENT_TIME_CONTRACT.md`.

## Cycle Phase 36 — owner-opted exact-time prenatal care reminders (59.0.0)

Optional `EXACT_TIME` Medicard reminder on a care item only when the owner has entered both `plannedDate` and `plannedTime` **and** explicitly selects exact mode. Not a medical scheduler. Adding Phase 35 `plannedTime` never auto-upgrades a Phase 33 DATE_BASED reminder (still 09:00 + `bumpOutOfQuiet`). Exact leads: at time / 30 min / 1 hour (default after explicit EXACT_TIME) / 2 hours. Removing time disables the reminder — no silent 09:00 fallback. Exact fire uses visit-alarm quiet-hours policy (no bump). Brain remains sole delivery. Calendar export unchanged; no OS alarms.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase36-exact-time-reminder/01-date-reminder-existing.png` … `12-small-screen.png`. Brain traces: `qa/cycle-phase36-exact-time-reminder/brain-traces.json`. Device restored (font 1.0, night no, wm size reset). iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PREGNANCY_CARE_EXACT_TIME_REMINDER_CONTRACT.md`.

## Cycle Phase 37 — owner-entered prenatal visit place (60.0.0)

Optional `plannedPlace` (max 160 Unicode) on an already-planned prenatal care date. User-authored visit-place text only — not verified provider data, not geolocation, not a map, not clinic search. Requires `plannedDate`. Clearing the date also clears time and place. Writes stay atomic on existing PUT `/api/cycle/pregnancy/care-plan/:careItemId`. Private note stays separate.

Reminders (DATE_BASED 09:00 and EXACT_TIME) ignore place. OS Calendar payload unchanged (`eventLocation=NULL`, no notes leak). Partner / Medi / doctor summary / analytics do not receive place. Personal export includes the exact string as user-entered text.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase37-visit-place/01-date-place-empty.png` … `12-small-screen.png`. Firewall: `qa/cycle-phase37-visit-place/firewall-traces.json`. Native Calendar: `native-events.txt` (`eventLocation=NULL`). Device restored (font 1.0, night no, wm size reset). iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/PREGNANCY_CARE_VISIT_PLACE_CONTRACT.md`.

## Cycle Phase 38 — postpartum AI fail-closed (safety closure)

`profileModeForAiPrompt(POSTPARTUM)` is now `null` (omitted), not `TRACK_PERIOD`. Cycle `CYCLE_WELLNESS` OpenRouter is not called. General Medi still runs without Cycle mode/reference/elapsed/stamped logs/forecasts. Peri still maps to TRACK. Contract: `docs/CYCLE_POSTPARTUM_MODE_CONTRACT.md`.

Explicit owner-selected `POSTPARTUM`. Not a pregnancy outcome. Optional owner-entered `referenceDate` (not birthDate). `CyclePostpartumEpisode` one ACTIVE. Forecast arithmetic unchanged; postpartum-stamped flow excluded at the engine input boundary. Presentation copies display-log `flow` onto calendar marks so bleed facts still paint.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase38-postpartum/01-mode-settings-postpartum.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset). Seed left on **bleed** / POSTPARTUM + reference `2026-08-19`. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_POSTPARTUM_MODE_CONTRACT.md`.

## Cycle Phase 40 — postpartum journal + empty-state copy isolation (63.0.0)

POSTPARTUM Journal and `/cycle/summary` preview shell no longer assume missing bleed is menstruation. Empty copy: `ამ თვალთვალის ინტერვალში ჩანაწერი ჯერ არ არის.` + CTA `ჩანაწერის დამატება`. Bleed label stays frozen `სისხლდენა`. Interactive `CyclePeriodHistory` hidden in POSTPARTUM (same capability boundary as Journal). TRACK empty still says `აღრიცხეთ მენსტრუაცია…`. No lochia/PPH/return-of-period. Phase 39 JSON/PDF unchanged. Forecast/AI/partner/Brain/schema/API unchanged. Presenter: `mobile/src/lib/cycleHistoryCopy.js`. Unknown mode does not fall back to TRACK copy.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase40-postpartum-copy/01-postpartum-journal-empty.png` … `12-small-screen.png`. Copy audit: `qa/cycle-phase40-postpartum-copy/copy-audit.json`. Device restored (font 1.0, night no, wm size reset). Seed left on **empty** / POSTPARTUM + reference `2026-08-19`. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_POSTPARTUM_COPY_ISOLATION_CONTRACT.md`.

## Cycle Phase 39 — postpartum doctor-summary context (62.0.0)

Additive `postpartumContext` on existing `GET /api/cycle/doctor-summary` when current mode is POSTPARTUM and an ACTIVE episode exists. Owner-entered `referenceDate` only; elapsed from canonical `postpartumElapsed`. Not a pregnancy outcome, not recovery scoring, not a diagnosis. Phase 38 AI fail-closed unchanged. Partner / export / Brain unchanged. No new endpoint or DB model.

Android 12 shots on Pixel_8 (`emulator-5554`): `qa/cycle-phase39-postpartum-doctor/01-postpartum-doctor-entry.png` … `12-small-screen.png`. Device restored (font 1.0, night no, wm size reset). Seed left on **ref** / POSTPARTUM + reference `2026-08-19`. iOS deferred. QA account: `cycle.qa.phase6@medicard.ge` / `CycleQaPhase6a`. Contract: `docs/CYCLE_POSTPARTUM_DOCTOR_SUMMARY_CONTRACT.md`.

## Admin ფაზები board (`#/cycleqa`)

Operator page under სისტემა that scans `qa/` and merges the freeze catalog. KPIs: current Cycle phase 37 / app 60.0.0 / frozen count / Android shot total. Per-phase detail: summary, P0/P1 fixes, leak-firewall flags (no `plannedPlace` samples), Android screenshots, artifacts, contract markdown. Does not show live user health. iOS QA remains product-deferred.

